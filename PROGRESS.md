# PROGRESS — DJAART SCHOOL

## Phase 0 — Initialisation du projet
- Statut : **terminée**

### Réalisé
- Dépôt GitHub créé (`Arthur-Djatche/djaart-school`, public), git local initialisé, push quotidien en place.
- Backend `djaart-school-backend/` : Laravel 12, Sanctum (auth SPA par cookie de session), Spatie Laravel-Permission (rôles), barryvdh/laravel-dompdf (prêt pour les phases de génération PDF).
  - `.env` configuré pour MySQL (`djaart_school`), CORS restreint au frontend, `bootstrap/app.php` avec `statefulApi()`.
  - Structure de dossiers posée : `Controllers/Api/{Auth,Parametrage,Finance,Inscription,Pedagogie,Documents}`, `Services/`, `Policies/`, `Requests/`, `Resources/`.
  - Auth minimale : `AuthController` (login/logout/me) + `LoginRequest`, réponses API standardisées (`data`/`message`/`errors`) via le trait `ApiResponse`, throttling sur `/login`.
  - Rôles de base créés (`super_admin`, `admin_etablissement`, `secretaire`, `comptable`, `enseignant`, `apprenant`) via `RoleSeeder`.
  - Utilisateur de test : `admin@djaart.school` / `password` (rôle `super_admin`) via `UserSeeder`.
  - Tests Feature (`AuthTest`, PHPUnit) : login valide/invalide, `/me` protégé, `/me` authentifié — 4/4 OK.
- Frontend `djaart-school-frontend/` : React + Vite + Tailwind v4, charte DJAART SCHOOL appliquée (logo + palette).
  - Structure : `api/`, `components/{ui,layout}`, `features/{auth,dashboard,parametrage,inscriptions,finance,pedagogie,documents}`, `hooks/`, `context/`, `routes/`, `utils/`.
  - `AuthContext` + `ToastContext`, `axiosClient` (cookies de session + XSRF), page de connexion, `DashboardLayout` (Topbar/Sidebar vide), routes protégées.
- Vérification bout-en-bout effectuée dans un vrai navigateur (Playwright) : connexion → dashboard de bienvenue → déconnexion → retour login, sans erreur bloquante.

### Hypothèses / écarts documentés par rapport au texte littéral du cahier des charges
- **Laravel 12** au lieu de "11.x" — version stable actuelle au moment du développement (juillet 2026), validé avec l'utilisateur.
- **Tailwind v4** au lieu de v3/`tailwind.config.js` — palette de marque déclarée via `@theme` dans `src/index.css`, pas de fichier `tailwind.config.js`. Validé avec l'utilisateur.
- **spatie/laravel-permission v6.x** (au lieu du dernier v8) — contrainte technique : l'environnement XAMPP local tourne en PHP 8.2.12, or spatie/laravel-permission v7 et v8 exigent PHP 8.3+. La v6.25 supporte Laravel 8→13 et PHP 8.0+, donc pleinement compatible avec Laravel 12 + PHP 8.2. Si PHP est mis à niveau vers 8.3+ dans XAMPP, une montée de version du package sera possible.
- **Authentification Sanctum en mode SPA (cookie/session)**, conforme à la mention `SANCTUM_STATEFUL_DOMAINS`/`SESSION_DOMAIN` de la section 3 du cahier des charges.
- Frontend et backend doivent être servis via le même nom d'hôte (`localhost`, pas `127.0.0.1`) pour que les cookies de session/CSRF Sanctum soient acceptés par le navigateur — `VITE_API_URL=http://localhost:8000` et `APP_URL=http://localhost:8000`.
- Extensions PHP `gd` et `zip` activées dans `C:\xampp\php\php.ini` (nécessaires pour dompdf/QR codes et la fiabilité de Composer).

### Comptes de démonstration
| Rôle | Email | Mot de passe |
|---|---|---|
| Super Admin | admin@djaart.school | password |

## Phase 1 — Authentification & gestion des utilisateurs/rôles
- Statut : **terminée**

### Réalisé
- Backend :
  - Table `etablissements` (minimale : nom, type, sigle) créée en avance de la Phase 2, uniquement comme ancrage FK multi-tenant — voir hypothèse ci-dessous.
  - `etablissement_id` nullable ajouté sur `users` (super_admin sans établissement, les autres rôles rattachés à un établissement).
  - `EtablissementScope` (Global Scope) + trait `BelongsToEtablissement`, réutilisables pour les futurs modèles métier (Phase 2+).
  - `UserController` (index paginé+recherche, store, update, destroy), `UserPolicy` (isolation par établissement, un `admin_etablissement` ne peut ni créer ni promouvoir un `super_admin`), `StoreUserRequest`/`UpdateUserRequest`, `UserResource`.
  - Middlewares Spatie (`role`, `permission`, `role_or_permission`) enregistrés dans `bootstrap/app.php`.
  - Mot de passe oublié : `PasswordResetController` (Laravel natif `Password` facade), URL de réinitialisation redirigée vers le frontend via `ResetPassword::createUrlUsing` dans `AppServiceProvider`.
  - Seeders étendus : établissement de démo "Lycée Démo DJAART" + un utilisateur par rôle restant.
  - Tests Feature : `UserManagementTest` (CRUD, isolation multi-établissement, restrictions de rôle), `PasswordResetTest` — 14/14 tests OK au total (suite complète).
- Frontend :
  - `config/navigation.js` (menu par rôle) + `Sidebar` dynamique, `ProtectedRoute` avec prop `roles` (redirection `/dashboard` si rôle non autorisé).
  - Composants réutilisables `Table` (recherche/tri/pagination) et `Modal`.
  - Pages `ForgotPasswordPage`/`ResetPasswordPage`, module `features/users` (liste + création/édition/suppression via modal).
- Vérification bout-en-bout (Playwright) : connexion successive avec les 6 comptes de démo, menu et accès à `/users` conformes au rôle pour chacun ; CRUD utilisateur complet (créer/modifier/supprimer) ; flux mot de passe oublié → réinitialisation → connexion avec le nouveau mot de passe.

### Bugs corrigés pendant cette phase (à retenir pour la suite)
- **Ne jamais appliquer un Global Scope qui appelle `Auth::user()`/`auth()->check()` sur le modèle `User` lui-même** : résoudre l'utilisateur authentifié interroge ce modèle, ce qui réapplique le scope et provoque une récursion infinie (plantage silencieux du serveur PHP, réponse 500 vide, sans log). `EtablissementScope` reste utilisable sur les futurs modèles métier (Classe, Inscription, Paiement...), mais **pas** sur `User` — le filtrage par établissement y est fait explicitement dans les contrôleurs.
- Middleware Spatie `role:` : plusieurs rôles se séparent avec `|` (ex. `role:super_admin|admin_etablissement`), **pas** avec une virgule (une virgule est interprétée comme un paramètre "guard" additionnel).
- `AuthContext` ne doit pas appeler `/api/me` sur les pages publiques (login, mot de passe oublié, reset) : cet appel touche la session et peut entrer en concurrence avec la requête de connexion sur la même session (pilote `database`), invalidant le jeton CSRF.
- Cohérence de dépaquetage des réponses Axios dans `authApi.js` : toutes les fonctions (`login`, `fetchMe`, ...) doivent unwrap `response.data` de la même façon avant de renvoyer l'enveloppe `{data, message, errors}` à l'appelant — une incohérence ici avait cassé la re-hydratation de session après un rechargement de page.
- Laravel 11+ : la classe `Controller` de base n'inclut plus le trait `AuthorizesRequests` par défaut — nécessaire pour `$this->authorize(...)`.

### Hypothèses / écarts documentés
- Modèle `Etablissement` créé en avance de la Phase 2 (voir ci-dessus), sous forme minimale ; la Phase 2 l'enrichira et ajoutera son écran de gestion CRUD dédié.

### Comptes de démonstration
| Rôle | Email | Mot de passe | Établissement |
|---|---|---|---|
| Super Admin | admin@djaart.school | password | — |
| Admin Établissement | admin_etablissement@djaart.school | password | Lycée Démo DJAART |
| Secrétaire | secretaire@djaart.school | password | Lycée Démo DJAART |
| Comptable | comptable@djaart.school | password | Lycée Démo DJAART |
| Enseignant | enseignant@djaart.school | password | Lycée Démo DJAART |
| Apprenant | apprenant@djaart.school | password | Lycée Démo DJAART |

## Phase 2 — Paramétrage académique
- Statut : **terminée**

### Réalisé
- Backend :
  - `Etablissement` enrichi (`adresse`) ; 5 nouveaux modèles (`AnneeAcademique`, `Filiere`, `Niveau`, `Classe`, `Matiere`), tous rattachés à un `etablissement_id` en dur et utilisant le trait `BelongsToEtablissement` (Global Scope) posé en Phase 1 — première utilisation réelle de cette infrastructure sur des modèles métier (sans le problème de récursion propre à `User`).
  - CRUD complet pour les 6 entités (`app/Http/Controllers/Api/Parametrage/`), avec le même pattern qu'en Phase 1 (Form Requests avec `etablissement_id` auto-assigné pour `admin_etablissement`/exigé pour `super_admin`, Policies via le trait partagé `ChecksEtablissementOwnership`, Resources dédiées).
  - Règles métier appliquées : `Niveau.type_systeme` (`classique`/`lmd`), `Classe` liée à un `Niveau` **et** une `AnneeAcademique` de le **même établissement** (validation croisée), `Filiere.code` unique par établissement (pas globalement), `AnneeAcademique.date_fin` postérieure à `date_debut`, `EtablissementPolicy` réservant `create`/`delete` au `super_admin` uniquement.
  - `ParametrageSeeder` : peuple entièrement le "Lycée Démo DJAART" existant (année, filière, 2 niveaux classiques, classes, matières) et crée **3 nouveaux établissements de démo** (primaire, universitaire LMD, centre de formation), chacun avec son propre `admin_etablissement` — couvrant les 4 types exigés par la section 5 du cahier des charges.
  - Tests Feature : `AcademicStructureTest` (auto-scoping par rôle, rejet cross-établissement, isolation des listes, unicité de code par établissement, validations métier) — 21/21 tests OK au total (suite complète).
- Frontend :
  - Module `features/parametrage/` (etablissements, annees, filieres, niveaux, classes, matieres), chaque page suivant le pattern posé par `features/users/` en Phase 1 (Table + Modal).
  - Nouveau composant `components/ui/Select.jsx` ; `Sidebar`/`config/navigation.js` étendus pour supporter un menu à sous-items ("Paramétrage").
  - Page `Niveaux` imbriquée sous une filière (`/parametrage/filieres/:filiereId/niveaux`) ; les formulaires Classe/Matière chargent dynamiquement la liste des niveaux disponibles.
- Vérification bout-en-bout (Playwright) : connecté en `admin_etablissement` (Lycée Démo DJAART), paramétrage **de zéro** d'une nouvelle filière → niveau → classe → matière ; isolation confirmée (un admin d'un autre établissement ne voit pas ces données) ; `super_admin` voit et gère les 4 établissements de démo.

### Bugs corrigés pendant cette phase
- Les migrations avec noms de table français pluralisés en préfixe (`annees_academiques`) ne correspondent pas à la pluralisation anglaise par défaut d'Eloquent (`Str::plural('AnneeAcademique')` → `annee_academiques`) : `protected $table` explicite nécessaire sur `AnneeAcademique`. Idem pour les FK générées par `foreignId()->constrained()` (deviné à partir du nom de colonne) : `->constrained('annees_academiques')` explicite requis.
- `Route::apiResource('classes', ...)` nomme le paramètre de route `{class}` (singulier anglais valide) au lieu de `{classe}` attendu par le contrôleur/Form Request — nécessite `->parameters(['classes' => 'classe'])`.
- Ordre des migrations : deux fichiers générés avec le même horodatage (`niveaux`/`classes`) peuvent s'exécuter dans le mauvais ordre alphabétique si les FK ne sont pas prises en compte — toujours vérifier/forcer l'ordre quand des dépendances de clé étrangère existent entre tables créées dans la même minute.

### Comptes de démonstration (ajoutés en Phase 2)
| Rôle | Email | Mot de passe | Établissement |
|---|---|---|---|
| Admin Établissement | admin.primaire@djaart.school | password | École Primaire Démo DJAART |
| Admin Établissement | admin.universite@djaart.school | password | Université Démo DJAART (LMD) |
| Admin Établissement | admin.centreformation@djaart.school | password | Centre de Formation Démo DJAART |

## Phase 3 — Frais de scolarité et tranches
- Statut : **terminée**

### Réalisé
- Backend :
  - Modèles `FraisScolarite` (unique par couple `Niveau`+`AnneeAcademique`, conforme à l'UML) et `Tranche`, tous deux avec le trait `BelongsToEtablissement`.
  - `FraisScolariteService` (`app/Services/`) : logique de calcul/transaction extraite du contrôleur (conforme section 7 du cahier des charges) — `createWithTranches` (mode `comptant` = 1 tranche automatique, mode `tranches` = lignes fournies) et `replaceTranches` (remplacement complet, utilisées toutes deux dans une transaction DB).
  - **Validation bloquante** dans `StoreFraisScolariteRequest`/`UpdateFraisScolariteRequest` : la somme des tranches doit égaler le montant total (tolérance d'arrondi 0,01) via une règle `withValidator`/`after` ; contrainte unique `(niveau_id, annee_academique_id)` en base, rejetée proprement en cas de doublon.
  - `FraisScolaritePolicy`, `FraisScolariteResource` (inclut les tranches), `FraisScolariteController`, mêmes patterns d'isolation multi-établissement qu'en Phase 2.
  - `FraisScolariteSeeder` : configure un barème (comptant ou tranches) pour chaque niveau des 4 établissements de démo, démontrant les deux modes.
  - Tests Feature : `FraisScolariteTest` (rejet somme incohérente, mode comptant, mode tranches valide, doublon niveau/année rejeté, isolation, remplacement via update) — 27/27 tests OK au total (suite complète).
- Frontend :
  - `src/api/financeApi.js`, module `features/finance/fraisScolarite/` (`FraisScolaritePage` + `FraisScolariteFormModal`).
  - **Répartition assistée** : bouton "Répartir automatiquement" qui calcule une répartition égale du montant total sur le nombre de tranches choisi, avec échéances mensuelles proposées à partir de la date de début de l'année académique — chaque ligne reste éditable, avec un total calculé en direct et un message d'erreur visuel si la somme ne correspond pas (miroir de la validation bloquante backend).
  - Nouveau groupe de navigation "Finance" (rôles `super_admin`/`admin_etablissement`).
- Vérification bout-en-bout (Playwright) : configuration d'un barème de zéro (nouvelle filière → niveau → frais en tranches via répartition assistée) réussie ; tentative de somme incohérente correctement bloquée avec message clair, sans enregistrement.

### Hypothèses / écarts documentés
- L'alternative A2 de l'UML ("modification d'une grille déjà utilisée par une inscription → nouvelle version plutôt qu'écrasement") ne s'applique pas encore : `Inscription` n'existe pas avant la Phase 4. Pour l'instant, `update` remplace directement les tranches existantes (`FraisScolariteService::replaceTranches`). **À revoir dès la Phase 4** : dès qu'une grille sera référencée par une inscription, protéger sa modification (avertissement ou versionnement) au lieu de l'écraser.

## Phase 4 — Inscriptions
- Statut : **terminée**

### Réalisé
- Backend :
  - Modèles `Apprenant` et `Inscription` (trait `BelongsToEtablissement`). Compteur `next_matricule_sequence` ajouté sur `etablissements` pour générer des matricules uniques et séquentiels (`{SIGLE}-{5 chiffres}`) sans collision sous concurrence (transaction + `lockForUpdate()`).
  - `ApprenantService` (génération de matricule, réutilisation/création d'apprenant) et `InscriptionService` (résolution automatique de la grille de frais à partir du couple niveau/année de la classe choisie, contrôle de capacité de la classe, détection automatique `nouvelle`/`reinscription`, annulation).
  - **Premier rôle opérationnel non-admin** introduit sur des routes protégées : `secretaire` peut désormais créer des inscriptions et consulter apprenants/classes/frais de scolarité (lecture seule sur ces deux derniers, écriture toujours réservée aux admins).
  - Tests Feature : `InscriptionTest` (matricule généré et séquentiel par établissement, grille de frais rattachée automatiquement, classe pleine rejetée, classe sans grille rejetée, isolation, rôle non autorisé rejeté, annulation, réinscription détectée, réutilisation d'apprenant existant) — 36/36 tests OK au total (suite complète).
- Frontend :
  - Module `features/inscriptions/` : liste des inscriptions + formulaire en une modale (recherche d'apprenant existant **ou** création à la volée, sélection de classe, aperçu automatique de l'échéancier de frais avant validation).
  - Nouvel item de navigation "Inscriptions" (rôles `super_admin`, `admin_etablissement`, **`secretaire`**).
- Vérification bout-en-bout (Playwright), connecté en `secretaire` : création d'un nouvel apprenant, inscription dans une classe avec grille de frais, échéancier affiché avant validation, matricule généré visible dans la liste.

### Bugs corrigés pendant cette phase
- **`next_matricule_sequence` absent du `$fillable` d'`Etablissement`** : `update()` l'ignorait silencieusement (protection mass-assignment, sans erreur), donnant toujours la séquence 1 — leçon générale : après l'ajout d'une colonne destinée à être modifiée par l'application (pas seulement en lecture), toujours vérifier qu'elle est bien listée dans `$fillable`, la lecture seule fonctionnant même sans cela.
- Règle de validation Laravel `prohibited_with` inexistante dans cette version : remplacée par une vérification manuelle dans `withValidator`/`after` (mutuelle exclusivité `apprenant_id`/`apprenant`).
- Routes `/classes` et `/frais-scolarite` (lecture) étaient enfermées dans le groupe réservé aux admins, rendant le formulaire d'inscription inutilisable pour `secretaire` : séparation de l'`index` (lecture, ouvert aussi à `secretaire`) des routes d'écriture (toujours admin uniquement), avec mise à jour des policies `viewAny` correspondantes.
- Généralisation du trait `ChecksEtablissementOwnership` : il vérifiait `hasRole('admin_etablissement')` en dur, ce qui aurait bloqué `secretaire` sur les vérifications de propriété d'établissement — remplacé par une simple comparaison d'`etablissement_id` (les méthodes `viewAny`/`create` de chaque policy filtrent déjà les rôles autorisés en amont).
- `components/ui/Modal.jsx` n'avait pas de défilement interne : un formulaire long (comme celui d'inscription) pouvait pousser le bouton de validation hors de l'écran sans moyen d'y accéder — ajout de `max-h-[90vh]` + `overflow-y-auto` sur le contenu, bénéficie à toutes les modales.

### Hypothèses / écarts documentés
- Cycle de vie de l'inscription partiel : seuls `en_cours` (création) et `annulee` (annulation manuelle) sont implémentés. Les transitions `validee` (Phase 5, premier paiement), `suspendue` et `cloturee` (Phase 7, clôture d'année) seront ajoutées avec leurs déclencheurs respectifs.
- Pas de gestion de liste d'attente pour une classe pleine : simple blocage avec message d'erreur.
- Champ `photo` de l'apprenant présent en base mais sans composant de téléversement dans l'UI (prévu pour la Phase 8, carte scolaire).

## Phase 5 — Paiements et reçus
- Statut : **terminée**

### Réalisé
- Backend :
  - Modèles `Paiement` et `Recu` (trait `BelongsToEtablissement`). Compteur `next_recu_sequence` ajouté sur `etablissements`, réutilisant le pattern atomique posé en Phase 4 (`next_matricule_sequence`) pour numéroter les reçus sans collision sous concurrence.
  - `RecuService::genererPour()` : transaction + `lockForUpdate()` sur l'établissement pour le numéro de reçu, génération PDF via `barryvdh/laravel-dompdf` (`resources/views/pdf/recu.blade.php`, charte DJAART SCHOOL appliquée), stockage sur `Storage::disk('local')` sous `recus/{etablissement_id}/{numero}.pdf`.
  - `PaiementService::encaisser()` : verrouille la tranche et l'inscription concernées, calcule le solde dû (somme des paiements déjà enregistrés **pour cette inscription précise**, cf. bug architectural ci-dessous), rejette un paiement sur tranche déjà soldée ou un montant dépassant le solde dû, délègue la génération du reçu, puis fait passer l'inscription de `en_cours` à `validee` dès que la 1ère tranche est intégralement réglée.
  - **Premier rôle opérationnel `comptable`** introduit sur des routes protégées : encaissement (`POST /paiements`), historique (`GET /paiements`), téléchargement de reçu (`GET /recus/{recu}/telecharger`), et lecture de `/apprenants` (recherche) + nouvel endpoint `GET /apprenants/{apprenant}/echeancier` (échéancier détaillé par inscription, avec statut de tranche calculé à la volée : `payee`/`partielle`/`en_attente`/`en_retard`).
  - **Garde-fou financier ajouté** : `FraisScolariteService::replaceTranches()` et `FraisScolariteController::destroy()` rejettent désormais toute modification/suppression d'une grille de frais dont au moins une tranche a déjà un paiement enregistré (`ValidationException` explicite) — résout concrètement l'hypothèse documentée en Phase 3/4 (UML A2) maintenant que des paiements réels peuvent exister.
  - `PaiementSeeder` (après `InscriptionSeeder`) : encaisse la 1ère tranche d'Aïcha Traoré intégralement (démontre la transition `en_cours` → `validee` + reçu n°1), et encaisse partiellement la tranche unique de Moussa Koné (démontre le statut `partielle` + reçu n°2).
  - Tests Feature : `PaiementTest` (paiement total → reçu généré, numéroté séquentiellement par établissement, inscription validée ; paiement partiel → statut `partielle` et solde correct via l'échéancier ; montant dépassant le solde rejeté ; paiement sur tranche déjà soldée rejeté ; rôle non autorisé rejeté ; isolation multi-établissement à la création **et** au téléchargement du reçu) — 45/45 tests OK au total (suite complète).
- Frontend :
  - `src/api/paiementApi.js` (échéancier, paiements, URL de téléchargement de reçu) ; `config/navigation.js`/`AppRoutes.jsx` étendus (groupe "Finance" gagne un lien "Caisse", rôles élargis à `comptable`, filtrage par rôle désormais possible au niveau de chaque sous-lien).
  - Module `features/finance/caisse/` (`CaissePage` + `PaiementFormModal`) : recherche d'apprenant (réutilise le pattern de `InscriptionFormModal`), échéancier par inscription avec badges visuels (teal=payée, orange=partielle, gris=en attente, rouge=en retard), encaissement en modale, lien de téléchargement immédiat du reçu après paiement, historique des paiements avec lien de reçu par ligne.
- Vérification bout-en-bout (Playwright), connecté en `comptable` : recherche d'un apprenant, échéancier affiché, encaissement du solde restant d'une tranche partiellement payée (statut → `payée`), téléchargement réel du reçu PDF via un vrai clic navigateur (fichier non vide, en-tête `application/pdf` confirmé), blocage vérifié pour un montant dépassant le solde dû (message clair, aucun enregistrement).

### Bugs corrigés pendant cette phase
- **Bug architectural (auto-détecté avant tout test)** : une `Tranche` est une ligne de grille **partagée** par toutes les inscriptions d'un même niveau+année (via `FraisScolarite`), pas un enregistrement par apprenant. Une première ébauche calculait `montant_payé`/`solde`/`statut` comme des accesseurs sur `Tranche` (somme de tous les paiements liés à la tranche, tous apprenants confondus) — un paiement d'un élève aurait compté dans le solde d'un autre élève partageant la même tranche. Corrigé avant tout test : accesseurs retirés de `Tranche` (ne garde qu'une relation `paiements()` documentée comme "ne pas utiliser pour calculer un solde individuel"), ajout de `Inscription::paiements()`, et tout le calcul de statut déplacé dans `ApprenantController::echeancier()`, explicitement scopé à une inscription à la fois.
- Même bug dans la première ébauche de `PaiementService` : tentative de déduire l'inscription à partir de la tranche via une relation `FraisScolarite::inscriptions()` inexistante. Corrigé en exigeant `inscription_id` explicitement dans la charge utile du paiement (`StorePaiementRequest`), avec validation croisée que la tranche appartient bien à la grille de frais de cette inscription.
- **Bug d'infrastructure (non lié au métier, découvert pendant la vérification navigateur du téléchargement de reçu)** : le lien de téléchargement utilisait `rel="noreferrer"` (réflexe sécurité habituel sur les liens `target="_blank"`), ce qui supprime l'en-tête `Referer` — or `Laravel\Sanctum\Http\Middleware\EnsureFrontendRequestsAreStateful` s'appuie sur cet en-tête pour reconnaître une requête comme provenant du frontend SPA et activer l'authentification par cookie de session. Sans lui, la requête était traitée comme non authentifiée. Corrigé en remplaçant par `rel="noopener"` (protège contre le tabnabbing sans supprimer le Referer) sur les deux liens de téléchargement de `CaissePage`.
- **Bug de robustesse découvert en creusant le bug précédent** : quelle qu'en soit la cause, toute requête `/api/*` non authentifiée et dépourvue d'en-tête `Accept: application/json` (navigation directe, lien copié/collé, session expirée puis lien de reçu réouvert) faisait planter le backend en 500 (`Route [login] not defined`) au lieu de renvoyer un 401 propre — ce backend est 100% API/SPA et n'a jamais défini de route nommée `login`. Corrigé dans `bootstrap/app.php` : `Middleware::redirectGuestsTo(fn () => null)` (empêche l'appel à `route('login')` au moment de la construction de l'exception dans le middleware `Authenticate`) + un renderable `AuthenticationException` dédié qui renvoie systématiquement un JSON 401 pour les chemins `/api/*`. Traité comme un correctif de fiabilité générale (pas spécifique aux reçus) compte tenu de la priorité absolue du projet sur la fiabilité des données financières.

### Hypothèses / écarts documentés
- **Alternative A2 de l'UML** ("montant > solde dû → système propose d'affecter le surplus à la tranche suivante") **non implémentée** : un paiement dépassant le solde restant de la tranche choisie est bloqué avec un message clair : le comptable doit saisir un montant ≤ solde dû ou encaisser la tranche suivante séparément. Répartition automatique du surplus déférée à une itération future si besoin.
- Le statut d'une tranche (`en_attente`/`partielle`/`payee`/`en_retard`) n'est **pas stocké** en base (pas de colonne `statut` sur `tranches`) : il est calculé à la volée par inscription (montant payé cumulé vs `montant`, `date_echeance` vs aujourd'hui) — évite tout risque de désynchronisation, cohérent avec le diagramme d'état de l'analyse UML.

## Correctif post-Phase 5 — Frais d'inscription et accès comptable aux inscriptions
- Statut : **terminé**

### Contexte
Retour utilisateur après la Phase 5 : le comptable doit pouvoir lui-même créer une inscription (cas d'usage : un parent se présente directement à la caisse), mais celle-ci ne doit être validée qu'après encaissement d'un montant de **frais d'inscription**, paramétrable, inclus dans la pension (pas un supplément). Ce montant remplace la règle provisoire posée en Phase 5 ("validée dès que la 1ère tranche est intégralement soldée").

### Réalisé
- Backend :
  - Nouveau champ `frais_inscription` sur `frais_scolarite` (paramétré par l'admin en même temps que `montant_total`/les tranches, requis, `<= montant_total`).
  - `PaiementService::validerInscriptionSiFraisInscriptionCouverts()` remplace l'ancienne règle basée sur la tranche n°1 : l'inscription passe à `validee` dès que le **cumul des paiements de l'apprenant sur cette inscription** atteint le montant des frais d'inscription — quelle que soit la tranche sur laquelle les paiements ont été enregistrés, même si aucune tranche n'est encore intégralement soldée.
  - Rôle `comptable` ajouté à `InscriptionPolicy` (`viewAny`/`create`) et à la route `POST /inscriptions` (le comptable peut désormais créer, lister et annuler des inscriptions, au même titre que la secrétaire).
  - Reçu PDF enrichi : ligne "Apprenant" remplacée par des lignes distinctes "Nom"/"Prénom"/"Matricule", ajout de la ligne "Année académique".
  - `FraisScolariteSeeder`/`PaiementSeeder` mis à jour pour démontrer concrètement la nouvelle règle (ex. Aïcha Traoré : paiement de 30 000 sur une tranche de 50 000, avec des frais d'inscription à 25 000 → inscription validée alors que la tranche reste "partielle").
  - Tests Feature : nouveau test dédié (`test_paiement_couvrant_les_frais_dinscription_valide_sans_solder_la_tranche`), `test_comptable_can_create_inscription`, `test_comptable_can_list_classes`, `test_comptable_can_list_frais_scolarite`, `test_frais_inscription_superieur_au_montant_total_est_rejete` — 50/50 tests OK au total (suite complète).
- Frontend :
  - `FraisScolariteFormModal`/`FraisScolaritePage` : champ et colonne "Frais d'inscription", avec message explicatif.
  - `InscriptionFormModal` : l'aperçu de l'échéancier affiche désormais le montant des frais d'inscription à encaisser pour valider l'inscription.
  - `comptable` ajouté aux rôles autorisés pour le module Inscriptions (navigation + routes protégées).
- Vérification bout-en-bout (Playwright) : connecté en `comptable`, création complète d'une inscription (apprenant + classe), aperçu affichant bien les frais d'inscription ; connecté en `admin_etablissement`, colonne "Frais d'inscription" visible dans la liste des grilles de frais.

### Bug corrigé pendant cette vérification (non détecté par la suite PHPUnit existante)
- En ouvrant le formulaire d'inscription en tant que `comptable` dans un vrai navigateur, la liste des classes restait vide malgré l'ajout du rôle à la route `GET /classes` : `ClassePolicy::viewAny()` et `FraisScolaritePolicy::viewAny()` vérifiaient encore une liste de rôles codée en dur n'incluant pas `comptable` (le contrôleur appelle `$this->authorize('viewAny', ...)` **en plus** du middleware de route — un même gap avait déjà été corrigé pour `ApprenantPolicy` en Phase 5, mais ces deux policies-ci avaient été oubliées). Corrigé en ajoutant `comptable` aux deux policies ; deux tests dédiés (`test_comptable_can_list_classes`, `test_comptable_can_list_frais_scolarite`) ajoutés pour que ce type de régression soit désormais couvert par la suite automatisée et non plus seulement détectable en navigateur.

## Phase 6 — Pédagogie : affectations et saisie des notes
- Statut : **terminée**

### Réalisé
- Backend :
  - Modèles `Sequence` et `Semestre` (paramétrés par niveau + année académique, exactement comme `FraisScolarite`), `AffectationEnseignant` (classe + matière + enseignant + année, unique par classe+matière+année), `Note` (`sequence_id`/`semestre_id` mutuellement exclusifs selon `Niveau.type_systeme`, `type_evaluation` `sequence`/`cc`/`session_normale`, `valeur` nullable + `absent`, `soumis_le` pour le verrouillage).
  - `NoteService::soumettre()` : transaction qui rejette tout lot déjà verrouillé (message clair), sinon crée/`updateOrCreate` une `Note` par apprenant et verrouille tout le lot en une fois (`soumis_le = now()`) — un seul geste "remplir puis soumettre", sans sauvegarde brouillon intermédiaire (MVP). `NoteService::deverrouiller()` : action admin dédiée qui remet `soumis_le` à `null` pour reprendre la saisie.
  - **Premier usage fonctionnel réel du rôle `enseignant`** (jusqu'ici un simple compte de démo) : peut lister ses propres affectations (`GET /affectations` auto-scopé par `enseignant_id` côté contrôleur), consulter/soumettre les notes de ses affectations uniquement (`AffectationEnseignantPolicy::voirNotes`/`soumettreNotes`, comparaison directe `enseignant_id === user->id`), lire séquences/semestres en lecture seule.
  - Champs `ponderation_cc`/`ponderation_session_normale` ajoutés à `Matiere` (somme = 100, validée), configurables dès cette phase mais **non utilisés pour un calcul** avant la Phase 7 (bulletins/relevés).
  - `PedagogieSeeder` : découpe chaque grille de frais déjà seedée (source des couples niveau+année réellement utilisés) en 3 séquences (niveaux classiques) ou 2 semestres (niveaux LMD) ; crée une affectation de démo (`enseignant@djaart.school` sur Mathématiques / 6ème A, Lycée Démo DJAART).
  - Tests Feature : `AffectationTest` (création admin, enseignant d'un autre établissement/sans le rôle rejeté, matière hors niveau de la classe rejetée, unicité classe+matière+année, enseignant ne voit que ses propres affectations, secrétaire sans accès), `SequenceTest`/`SemestreTest` (CRUD admin, unicité par niveau+année+numéro, isolation, lecture enseignant), `NoteTest` (soumission verrouille, re-soumission bloquée tant que non déverrouillée, enseignant non titulaire rejeté, déverrouillage admin puis nouvelle soumission acceptée, note "absent" sans valeur, CC et Session Normale = verrous indépendants sur le même semestre, rôle non listé rejeté) — 71/71 tests OK au total (suite complète).
- Frontend :
  - `src/api/pedagogieApi.js` ; nouveau groupe de navigation "Pédagogie" avec accès différencié par sous-lien (Affectations/Séquences/Semestres = admins, Saisie des notes = + `enseignant`), même principe que le groupe "Finance".
  - Écrans admin `AffectationsPage`/`SequencesPage`/`SemestresPage` (mêmes patterns Table/Modal que le reste du paramétrage) ; `MatiereFormModal` étendu avec les deux champs de pondération CC/SN.
  - `SaisieNotesPage` (enseignant) : sélection d'une affectation puis, selon le type de système du niveau, d'une séquence (classique) ou d'un semestre + type d'évaluation CC/SN (LMD) ; grille apprenant × note (+ case "Absent") ; une fois verrouillée, la grille repasse en lecture seule avec message explicite.
- Vérification bout-en-bout (Playwright) : connecté `admin_etablissement`, affectation et séquence de démo visibles ; connecté `enseignant@djaart.school`, saisie d'une note pour l'unique apprenant de "6ème A" → soumission → verrouillage confirmé (grille désactivée, bouton "Soumettre" disparu) ; déverrouillage admin confirmé (`soumis_le` repassé à `null` en base) ; tentative de soumission par un utilisateur non titulaire de l'affectation rejetée (403).

### Hypothèses / écarts documentés
- **Séquence**/**Semestre** configurés par niveau + année académique (comme `FraisScolarite`), pas au niveau de l'établissement entier — permet à chaque niveau d'avoir son propre découpage si besoin, cohérent avec le reste du système.
- **Absence** : stockée comme un booléen `absent` (valeur nulle) plutôt que forcée à 0 — le traitement dans le calcul de moyenne (0 ou neutralisée) est différé à la Phase 7, qui ne fait pas encore partie du périmètre ici (Phase 6 = saisie et verrouillage uniquement, pas de calcul de moyenne/bulletin).
- Hors périmètre explicite (alternatives UML différées) : blocage automatique de la saisie Session Normale pour "assiduité insuffisante" (aucun suivi d'assiduité n'existe dans le projet) ; calcul de moyenne UE/matière, rang, mention — tout cela est Phase 7.

## Phase 7 — Bulletins et relevés de notes
- Statut : **terminée**

### Réalisé
- Backend :
  - Modèles `Bulletin` (`inscription_id`, `sequence_id`, `moyenne_generale`, `rang`, `fichier_pdf`, unique par inscription+séquence) et `ReleveDeNotes` (`inscription_id`, `semestre_id` nullable — null pour le relevé annuel classique — `moyenne_generale`, `mention`, `fichier_pdf`).
  - `BulletinService::cloturerSequence(classe, sequence)` : génère **en un seul lot** les bulletins de toute la classe (le rang exige de comparer tous les apprenants actifs entre eux) — vérifie que toutes les matières affectées ont une saisie **verrouillée** couvrant tous les apprenants actifs (sinon bloque avec la liste des matières incomplètes) ; moyenne pondérée par coefficient (note "Absent" = 0, résout l'hypothèse différée en Phase 6) ; rang par classement de compétition (ex æquo = même rang) ; PDF par apprenant (même pattern dompdf que `RecuService`) ; bloque toute re-clôture si des bulletins existent déjà.
  - `ReleveService::genererAnnuelClassique(classe)` : vérifie qu'un bulletin existe pour **chaque séquence** du niveau/année avant de générer (moyenne = moyenne des moyennes de séquence, mention selon barème classique). `ReleveService::genererSemestrielLmd(classe, semestre)` : vérifie que chaque UE a des notes **CC et Session Normale** verrouillées ; moyenne UE = pondération `ponderation_cc`/`ponderation_session_normale` posée en Phase 6 ; moyenne générale pondérée par `credits_ects` ; mention Admis/Ajourné.
  - `BulletinPolicy`/`ReleveDeNotesPolicy` (`super_admin`, `admin_etablissement`, `secretaire` — la secrétaire déclenche la clôture comme le prévoit l'UML) ; routes imbriquées `POST /classes/{classe}/sequences/{sequence}/cloturer`, `POST /classes/{classe}/releves/annuel`, `POST /classes/{classe}/semestres/{semestre}/releves`.
  - Tests Feature : `BulletinTest` (clôture bloquée si notes manquantes avec message nommant la matière, moyenne pondérée et rang corrects sur 2 apprenants, re-clôture bloquée, note "Absent" comptée comme 0, rôle enseignant/autre établissement rejeté), `ReleveTest` (relevé classique bloqué si séquence sans bulletin, mention correcte, relevé LMD bloqué si CC/SN manquant, moyenne pondérée par crédits ECTS et mention Admis/Ajourné correctes, secrétaire autorisée, comptable rejeté) — 83/83 tests OK au total (suite complète).
- Frontend :
  - `src/api/bulletinsApi.js` ; groupe "Pédagogie" étendu avec "Bulletins" et "Relevés de notes" (rôles `super_admin`/`admin_etablissement`/`secretaire`).
  - `BulletinsPage` : classe → séquence → "Clôturer la séquence" → tableau des bulletins (moyenne, rang, téléchargement). `RelevesPage` : classe → (si niveau LMD) semestre → génération en un clic pour toute la classe → tableau des relevés (moyenne, mention, téléchargement).
- Vérification bout-en-bout (Playwright) : `enseignant@djaart.school` saisit et verrouille les 3 séquences de "6ème A" ; `admin_etablissement` clôture chacune (bulletins générés, moyenne/rang corrects), re-clôture bloquée avec message clair, téléchargement réel du bulletin PDF via un vrai clic navigateur (fichier non vide) ; génération du relevé annuel réussie une fois les 3 séquences clôturées, moyenne et mention correctes.

### Bug corrigé pendant cette vérification
- **Convention de nommage des policies** : `ReleveDeNotesPolicy` avait été nommée `RelevePolicy` — Laravel 11 résout les policies par correspondance exacte `{NomDuModèle}Policy` (ici `ReleveDeNotes` → `ReleveDeNotesPolicy`), sans quoi `$this->authorize(..., ReleveDeNotes::class)` échoue silencieusement et refuse l'accès à **tout le monde**, y compris les admins — masqué en tests unitaires jusqu'à ce que `ReleveTest` échoue sur des cas pourtant correctement autorisés. Corrigé en renommant fichier et classe pour respecter la convention.
- **Grille de saisie non réinitialisée en changeant de séquence** (`SaisieNotesPage.jsx`) : `chargerGrille()` ne vidait pas l'ancien état `grille` avant de récupérer les notes de la séquence nouvellement sélectionnée, laissant transitoirement affichée (et verrouillée) la grille de la séquence précédente. Corrigé en réinitialisant `grille` à `null` au début du chargement (affiche "Chargement…" pendant la requête) — découvert en testant la saisie successive de plusieurs séquences dans la même session navigateur.

### Hypothèses / écarts documentés
- **Barème de mention non chiffré dans le cahier des charges** (choix assumé) : classique — Excellent ≥16, Bien ≥14, Assez Bien ≥12, Passable ≥10, Insuffisant <10 ; LMD — Admis si moyenne générale ≥10, sinon Ajourné (pas de session de rattrapage, déjà différée en Phase 6).
- **Génération en lot par classe** (pas apprenant par apprenant) pour bulletins et relevés — le rang et la vérification de complétude s'évaluent naturellement à l'échelle de la classe.
- **Portail apprenant/parent hors périmètre** : consultation réservée à secrétariat/admin (aucune authentification apprenant/parent n'existe dans ce projet, cf. même remarque en Phase 5 pour les reçus).

## Phase 8 — Effets académiques (attestations et cartes)
- Statut : **terminée**

### Réalisé
- Backend :
  - Dépendance `endroid/qr-code` ajoutée (génération de QR en PNG via le backend GD déjà activé depuis la Phase 0), sans conflit avec la contrainte PHP 8.2 du projet.
  - Modèles `Attestation` (`type` scolarité/fréquentation/réussite, `numero` séquentiel par établissement) et `CarteScolaire` (`numero` séquentiel + `numero_duplicata` par apprenant, `date_emission`/`date_expiration`).
  - `AttestationService::generer()` et `CarteScolaireService::generer()` : vérifient qu'une inscription `validee` existe pour l'apprenant sur l'année académique `en_cours` de son établissement (sinon bloquent) ; réutilisent le pattern de compteur atomique déjà posé en Phases 4/5/7 (`next_attestation_sequence`, `next_carte_sequence`) ; génèrent un PDF (dompdf, même charte DJAART SCHOOL) avec un QR code auto-porteur (`QrCodeService`, encode les données d'identification du document en texte, pas une URL de vérification publique).
  - `CarteScolaireService` bloque en plus si l'apprenant n'a pas de photo, et calcule `numero_duplicata` (0 pour la première carte, incrémenté à chaque réémission) tout en donnant systématiquement un `numero` séquentiel neuf.
  - **Premier upload de fichier du projet** : `ApprenantController::updatePhoto()` (disque `public`, `php artisan storage:link` exécuté une fois pour exposer les fichiers) — remplace l'ancienne photo si une nouvelle est téléversée.
  - `ApprenantPolicy::gererDocuments()` (nouvelle capacité) : secrétariat/admins uniquement (pas le comptable, hors de son ressort financier).
  - Tests Feature : `AttestationTest` (blocage si inscription non validée, numérotation séquentielle, téléchargement PDF, rôles enseignant/comptable rejetés, isolation multi-établissement), `CarteScolaireTest` (blocage si pas de photo, génération réussie après upload, réémission incrémentant `numero_duplicata` avec un nouveau `numero`, isolation multi-établissement) — 94/94 tests OK au total (suite complète).
- Frontend :
  - **Première "fiche apprenant" du projet** (`ApprenantsListPage` + `ApprenantFichePage`, nouveau lien de navigation top-level "Apprenants") : jusqu'ici la recherche d'apprenant n'existait qu'en ligne dans des modales (inscription, caisse), sans page de profil autonome.
  - `ApprenantFichePage` : affichage/upload de photo, génération d'attestation (sélection du type) et de carte scolaire en un clic, historique combiné des documents déjà émis avec lien de téléchargement et badge de duplicata.
- Vérification bout-en-bout (Playwright), connecté `secretaire@djaart.school` : upload de photo pour Aïcha Traoré, génération d'une attestation de scolarité (PDF téléchargé réel, non vide), génération d'une carte scolaire (PDF avec QR et photo intégrés), réémission d'une seconde carte → badge "Duplicata 1" confirmé ; création en direct d'une nouvelle inscription non payée → tentative de génération d'attestation correctement bloquée avec message clair.

### Hypothèses / écarts documentés
- **QR auto-porteur, pas d'URL de vérification publique** : aucun portail de consultation/vérification externe n'existe dans ce projet (même remarque que le portail apprenant différé en Phases 5/7) — le QR encode directement les données d'identification du document.
- **Blocage strict** (pas d'attestation "sous réserve") si l'inscription n'est pas validée, cohérent avec le choix déjà fait en Phases 5/7.

## Phase 9 — Tableaux de bord et rapports
- Statut : **terminée**

### Contexte et décision de périmètre
Le cahier des charges demande un tableau de bord par rôle, y compris "Apprenant". Or aucune liaison `User`↔`Apprenant` n'existe dans ce projet (déjà documenté comme différé en Phases 5, 7 et 8, sous l'angle du portail apprenant/parent). Avant de re-différer une 4ème fois par simple hypothèse silencieuse, la question a été posée explicitement à l'utilisateur : le tableau de bord "Apprenant" reste **hors périmètre** pour cette itération (décision confirmée). Cette phase livre les 4 tableaux de bord **staff** : Super Admin, Admin établissement, Comptable, Enseignant.

### Réalisé
- Backend :
  - **Un seul endpoint `GET /dashboard`** (pas de restriction de rôle sur la route elle-même, ouverte à tout utilisateur authentifié) : le contenu varie selon le rôle déterminé côté serveur (`DashboardController` teste `hasRole()` dans l'ordre super_admin → admin_etablissement → comptable → enseignant) et renvoie `{ role, data }`. Plus simple qu'un endpoint par rôle ; le frontend n'a qu'à choisir le composant de présentation correspondant à `role`.
  - `DashboardService` : une méthode par rôle.
    - `pourSuperAdmin()` : agrégats globaux (nb établissements, apprenants actifs tous établissements, encaissé du mois tous établissements, effectif d'utilisateurs par rôle via le scope Spatie `User::role()`) + un tableau par établissement (effectif, encaissé du mois) — bypass volontaire du scope multi-tenant, réservé à ce rôle.
    - `pourAdminEtablissement()` : effectifs par classe, taux de recouvrement (encaissé / `montant_total` attendu sur les inscriptions actives, tous statuts confondus hors annulée), liste des affectations dont la **période la plus récente** (dernière séquence ou dernier semestre configuré pour le niveau+année) n'a pas encore de saisie verrouillée, top-5 des impayés en retard.
    - `pourComptable()` : encaissé du jour, encaissé du mois, liste complète des impayés en retard.
    - `pourEnseignant()` : ses affectations avec, pour chacune, la période la plus récente et si elle est déjà soumise ou non.
  - `RapportService` (réutilisé par `DashboardService` pour la liste d'impayés, afin de ne pas dupliquer la règle de calcul) :
    - `listerImpayes()` : tranches dont `date_echeance` est dépassée et dont le solde (montant de la tranche − somme des paiements de **cette inscription** sur cette tranche, même méthode que `ApprenantController::echeancier`) est encore positif — agrégées par apprenant, triées par ancienneté de retard décroissante.
    - `statistiquesReussite()` : répartition des mentions déjà attribuées, à partir de `ReleveDeNotes.mention` (et non `Bulletin`, qui ne porte qu'une moyenne/rang par séquence sans mention).
  - **Rapports en PDF uniquement**, générés à la volée et streamés directement (`response()->streamDownload` équivalent — réponse PDF brute avec `Content-Disposition: attachment`), **non persistés en base** : ce sont des rapports consultatifs, pas des documents officiels numérotés comme les reçus/attestations/bulletins. `RapportController::impayes()`/`statistiquesReussite()`, route `role:super_admin|admin_etablissement|comptable` (l'enseignant et la secrétaire n'y ont pas accès).
  - Tests Feature : `DashboardTest` (agrégats globaux corrects pour super_admin sur 2 établissements, taux de recouvrement et top impayés corrects pour admin_etablissement avec isolation multi-établissement vérifiée, encaissements du jour/mois et impayés corrects pour comptable, affectations avec statut de saisie correct pour enseignant, isolation des affectations incomplètes entre établissements), `RapportTest` (PDF valides pour comptable/admin_etablissement/super_admin, accès refusé à enseignant et secrétaire) — 104/104 tests OK au total (suite complète).
- Frontend :
  - `src/api/dashboardApi.js` (`fetchDashboard`, URLs des deux rapports).
  - `DashboardPage.jsx` remplace le message générique de bienvenue : bascule sur l'un des 4 composants (`SuperAdminDashboard`, `AdminEtablissementDashboard`, `ComptableDashboard`, `EnseignantDashboard`, nouveau dossier `features/dashboard/`) selon `data.role` reçu de l'API. Cartes de métriques (`MetricCard`, couleurs de marque) + tableaux simples pour les listes (impayés, affectations incomplètes, effectifs, par établissement).
  - Boutons d'export PDF ("Rapport des impayés", "Statistiques de réussite") sur les tableaux de bord Admin établissement/Comptable, ouverture dans un nouvel onglet avec `rel="noopener"` (cohérent avec le correctif Referer/Sanctum de la Phase 5).
- Vérification bout-en-bout (Playwright, sur les données de démo existantes) : connexion successive `admin@djaart.school` (super_admin — 4 établissements, répartition par rôle et par établissement cohérente), `admin_etablissement@djaart.school` (taux de recouvrement 27,6 %, 1 affectation à saisir, impayés réels affichés), `comptable@djaart.school` (encaissements du jour/mois et impayés en retard identiques à ceux de l'admin établissement, même établissement), `enseignant@djaart.school` (affectation "6ème A / Mathématiques", statut "À saisir" correct) — aucune erreur console sur les 4 tableaux de bord ; téléchargement réel des deux rapports PDF pour l'admin établissement (contenu non vide, en-tête `%PDF` valide).

### Hypothèses / écarts documentés
- **Tableau de bord Apprenant explicitement hors périmètre** (décision utilisateur confirmée, cf. contexte ci-dessus) — aucune route ni composant n'est prévu pour ce rôle dans cette phase.
- **Rapports PDF uniquement**, pas d'export Excel/CSV séparé — le "/" du cahier des charges ("PDF/Excel") a été lu comme une alternative de format, pas une obligation des deux ; évite d'ajouter une dépendance lourde (type PhpSpreadsheet) pour un besoin secondaire.
- **Rapports non persistés/non numérotés** (contrairement aux reçus/attestations/cartes/bulletins) — générés à la demande à chaque téléchargement, cohérents avec leur nature consultative plutôt qu'officielle.
- **Statistiques de réussite basées sur `ReleveDeNotes.mention`**, pas `Bulletin` (qui n'a qu'une moyenne/rang par séquence, sans mention) — plus juste par rapport aux données réellement disponibles.

## Correctif majeur — Branding, documents stylés, génération en masse, matricule à l'échelle
- Statut : **terminé**

### Contexte
Retour utilisateur : le système doit gérer des milliers d'apprenants, ce qui exposait plusieurs limites : matricule à longueur/capacité non maîtrisées (`{sigle}-00001`, 5 chiffres, sigle de longueur libre), documents PDF fonctionnels mais sommaires (pas de logo/signature d'établissement), génération de documents un par un (attestations/cartes) alors que les bulletins/relevés étaient déjà en lot, et un reçu qu'il fallait cliquer/télécharger manuellement avant de pouvoir l'imprimer. L'utilisateur a aussi fourni 5 spécimens réels (bulletins annuel/séquentiel secondaire et primaire, carte d'étudiant, relevé universitaire) comme référence de design **et de contenu**. Décision validée (AskUserQuestion) : reproduire aussi les concepts métier qui y apparaissent (groupes de matières, professeur principal, conduite/mentions travail) plutôt qu'un simple restylage visuel.

### Réalisé
- **Image de marque de l'établissement** : `Etablissement.logo`/`signature` (upload image, pattern identique à la photo d'apprenant Phase 8), routes `POST /etablissements/{id}/logo` et `/signature` (autorisation via la policy `update` existante), embarqués en base64 dans tous les PDF via un trait partagé `Concerns/EmbedsEtablissementBranding` (réutilisé par les 5 services de génération de documents).
- **Matricule à l'échelle** : nouveau format **2 lettres majuscules (code établissement) + 8 chiffres = 10 caractères exactement** (`ApprenantService::generateMatricule`), capacité 99 999 999 apprenants par établissement (contre 99 999 avec l'ancien format `{sigle}-00001`, dont la longueur dépendait en plus du sigle saisi librement). Ne s'applique qu'aux nouveaux apprenants — les matricules déjà émis ne sont jamais renumérotés.
- **Nouveaux concepts pédagogiques (système classique uniquement, cohérent avec les spécimens fournis)** :
  - `Matiere.groupe` (libellé libre, ex. "Groupe I") → sous-totaux par groupe sur le bulletin.
  - `Classe.professeur_principal_id` → affiché comme "Titulaire" sur le bulletin.
  - `Apprenant.lieu_naissance` → complète "Né(e) le … à …" sur bulletins/relevés/attestations/cartes.
  - Nouveau modèle `ConduiteReleve` (absences, absences non justifiées, retards, mentions travail/conduite parmi tableau d'honneur/encouragements/avertissement/blâme) saisi via un nouvel écran dédié `SaisieConduitePage` (classe → séquence → grille par apprenant, **non verrouillé** comme les notes — modifiable librement tant que la séquence n'est pas clôturée) ; figé sur le `Bulletin` au moment de la clôture (mêmes valeurs par défaut à zéro/`null` si non saisi, **non bloquant**).
  - `BulletinService::cloturerSequence` étendu (sans toucher au calcul moyenne/rang déjà testé) : sous-totaux par groupe (`details_groupes` JSON), statistiques de classe (`moyenne_classe`, `taux_reussite`, `moyenne_max`, `moyenne_min`), conduite figée — tout calculé à partir des données déjà en mémoire dans la méthode, sans requête supplémentaire lourde.
- **Refonte visuelle des 5 documents PDF** (bulletin, relevé, reçu, attestation, carte scolaire) : en-tête avec logo, zone de signature avec l'image de signature, tableaux à bordures nettes et encadrés d'info inspirés des spécimens fournis — sans reproduire les mentions gouvernementales spécifiques à un pays (plateforme multi-établissements généraliste).
- **Génération en masse (attestations et cartes scolaires)** : nouveaux endpoints `POST /classes/{classe}/attestations/masse` et `/cartes-scolaires/masse` (ciblent toute la classe ou une liste `apprenant_ids`, échecs individuels capturés par apprenant sans interrompre le lot), endpoints ZIP (`GET /attestations/zip?ids=...`, `/cartes-scolaires/zip?ids=...`, `ZipArchive` natif, aucune nouvelle dépendance) pour un téléchargement groupé en un clic. Nouvelle page `DocumentsMassePage` (sélection classe → cases à cocher → génération → résultats détaillés → téléchargement ZIP). La fiche apprenant individuelle reste disponible pour les régénérations ponctuelles.
- **Impression automatique du reçu** : `RecuController::telecharger` accepte `?inline=1` (affichage direct au lieu du téléchargement forcé) ; `CaissePage` réserve une fenêtre (`window.open('', '_blank')`) **avant** l'appel asynchrone d'encaissement (dans le même geste utilisateur que le clic "Encaisser", pour ne pas être bloqué comme pop-up non sollicité — même leçon que le correctif Referer de la Phase 5), puis la redirige vers une nouvelle page `/imprimer-recu/:recuId` qui affiche le reçu en plein écran et déclenche `window.print()` au chargement. La boîte de dialogue d'impression du navigateur reste incontournable (aucune techno web ne permet d'imprimer silencieusement) — c'est le clic "Télécharger" intermédiaire qui disparaît, pas la boîte de dialogue elle-même.
- Tests Feature : nouveaux tests pour le format de matricule (regex `^[A-Z]{2}[0-9]{8}$`, repli `XX` si sigle vide), l'upload/l'autorisation/l'isolation du logo et de la signature (`EtablissementBrandingTest`), le CRUD/la policy/l'isolation de `ConduiteReleve` (professeur principal autorisé, enseignant non titulaire refusé, saisie bloquée après clôture), les calculs `BulletinService` étendus (sous-totaux de groupe et statistiques de classe vérifiés avec des valeurs numériques exactes, conduite figée avec valeurs par défaut si non saisie), la génération en masse (succès partiel rapporté ligne par ligne, ZIP valide, isolation multi-établissement), le reçu inline vs attachment — 127/127 tests OK au total (suite complète).
- Vérification bout-en-bout (Playwright) : upload logo+signature de l'établissement (aperçus corrects) ; ajout d'une 2e affectation (Français) pour tester des sous-totaux de groupe distincts ; saisie de la conduite pour "6ème A"/Séquence 1 ; clôture de la séquence → bulletin PDF redessiné téléchargé (1 Mo, logo/signature embarqués, `%PDF` valide) ; génération en masse d'attestations pour toute la classe → résultats détaillés → téléchargement du ZIP (valide, en-tête `PK`) ; encaissement d'un paiement → nouvel onglet ouvert automatiquement sur `/imprimer-recu/:id` sans clic intermédiaire, aucune erreur console sur l'ensemble du parcours.

### Bug corrigé pendant cette vérification
- **`PaiementSeeder` cassé par le nouveau format de matricule** : le seeder recherchait les apprenants de démo par leur ancien matricule codé en dur (`'LDD-00001'`/`'LDD-00002'`), introuvable après le changement de format ; `migrate:fresh --seed` échouait avec `ModelNotFoundException`. Corrigé en mettant à jour les valeurs codées en dur vers le nouveau format (`'LD00000001'`/`'LD00000002'`) — aucune autre référence à l'ancien format trouvée ailleurs dans les seeders.

### Hypothèses / écarts documentés
- **Logo et signature appartiennent à l'Établissement**, pas à un utilisateur précis : "sa signature numérique" a été interprété comme l'image de signature officielle de l'institution (comme "le logo de son établissement" dans la même phrase), pas une signature cryptographique — cohérent avec le pattern déjà établi pour la photo d'apprenant.
- **Groupes de matières / professeur principal / conduite : uniquement pour le système classique** — le système LMD garde son fonctionnement actuel (UE via `credits_ects`), cohérent avec le spécimen universitaire fourni qui n'a ni conduite ni groupes.
- **Mentions travail/conduite saisies manuellement**, pas calculées à partir du rang — dans les spécimens, ce sont des choix du conseil de classe, pas une formule automatique.
- **Génération en masse par classe** (comme les bulletins/relevés), pas par établissement entier — cohérent avec le seul pattern déjà existant dans l'interface.

## Affinage — En-tête/filigrane, bulletin enrichi, spécialités universitaires, reçu
- Statut : **terminé**

### Contexte
Retour utilisateur après le correctif branding : plutôt qu'un logo réinjecté dans un en-tête généré par l'application, l'admin doit pouvoir importer **l'en-tête complet déjà prêt** de ses documents (spécimen fourni : `entete.jpeg`, bandeau bilingue logo+texte+sceau) — l'en-tête généré automatiquement ("logo + nom établissement") n'est alors plus affiché. Le logo seul sert désormais de **filigrane** en fond de tous les documents. Le bulletin secondaire se rapproche encore des spécimens (nom de l'enseignant sous chaque matière, appréciation par matière, tableau d'honneur automatique), les universités gèrent leurs spécialités avec un chef de département, et le reçu affiche le solde restant avec un espace de signature manuelle plutôt que l'image de signature de l'établissement.

### Réalisé
- **En-tête complet + filigrane** : `Etablissement.entete` (upload image, même pattern que logo/signature, `EtablissementController::updateEntete` factorisé via une méthode privée commune `updateImage`), trait `EmbedsEtablissementBranding::enteteDataUri()`. Les 5 templates PDF affichent l'en-tête importé en pleine largeur à la place du bloc "logo + nom" s'il existe (repli sinon) ; le logo est positionné en filigrane (`position: fixed`, opacité ~0.07, `z-index:-1`) en fond de chaque document — sauf sur la carte scolaire où l'en-tête complet n'est pas utilisé (format trop compact pour un bandeau conçu pour une page A4 ; le logo y reste filigrane).
- **Bulletin enrichi (système classique uniquement)** :
  - Nom de l'enseignant affiché sous chaque matière (comme le spécimen).
  - **Barème d'appréciation par matière** reconstruit et vérifié un par un sur les 12 couples (note, appréciation) du spécimen `bulletin_annuel_secondaire.jpeg` : `<5` Très Faible · `[5,8[` Faible · `[8,9.5[` Insuffisant · `[9.5,10[` À peine passable · `[10,12[` Passable · `[12,14[` Assez Bien · `[14,16[` Bien · `≥16` Excellent (le "11,75 → Passable" et "12-13 → Assez Bien" cités par l'utilisateur en sont des exemples exacts : Anglais 11,75 et EPS 13,50 dans le spécimen).
  - **Tableau d'honneur automatique** (`Bulletin.tableau_honneur`, calculé `moyenne_generale >= 12`) : remplace la valeur `tableau_honneur` qui existait dans le menu manuel "Mention travail" de `SaisieConduitePage` — celle-ci ne porte plus que encouragements/avertissement/blâme, choix réellement discrétionnaires. Migration dédiée qui neutralise d'abord les lignes existantes portant l'ancienne valeur avant de restreindre l'ENUM (MySQL uniquement ; SQLite/tests s'appuie sur la validation applicative).
- **Spécialités universitaires** : `Filiere.chef_departement_id` (même pattern que `professeur_principal` sur `Classe` — doit être un enseignant du même établissement), le concept de "spécialité" s'appuyant sur la `Filiere` déjà existante (ex. "Informatique" pour Université Démo DJAART) plutôt qu'un nouveau concept.
- **Reçu** : ajout du solde restant sur la tranche (même calcul que `ApprenantController::echeancier`) ; remplacement de l'image de signature de l'établissement par un espace vierge avec la mention "Signature du caissier" — le reçu est signé à la main par le caissier, pas par l'admin.
- Tests Feature : upload de l'en-tête (`EtablissementBrandingTest`), barème d'appréciation vérifié par `#[DataProvider]` sur les 12 valeurs exactes du spécimen, tableau d'honneur automatique (vrai à 12, faux à 11,99), reçu inline avec `Content-Disposition` vérifié, chef de département (accepté si enseignant du même établissement, rejeté sinon) — 145/145 tests OK au total (suite complète).
- Vérification bout-en-bout (Playwright) : upload de l'en-tête réel fourni par l'utilisateur (aperçu conforme) ; ajout d'une 2e affectation (Français) pour enrichir le bulletin ; chef de département assigné sur une filière ; clôture de séquence → bulletin PDF redessiné (27 Ko, en-tête + filigrane + appréciations + enseignants) ; encaissement → reçu avec solde restant (23 Ko) ; aucune erreur console sur l'ensemble du parcours.

### Bug de test corrigé pendant cette vérification (pas un bug applicatif)
- Le script de vérification navigateur vérifiait le contenu de la page "Affectations" immédiatement après un clic de navigation, avant que l'appel API asynchrone n'ait eu le temps de se résoudre — la page réelle fonctionnait correctement (confirmé en l'observant directement avec une attente suffisante), seul le script de test était trop impatient. Corrigé en attendant la disparition du message "Chargement…" avant d'inspecter le contenu.

### Hypothèses / écarts documentés
- **Logo et en-tête appartiennent à l'Établissement**, pas à un utilisateur précis — cohérent avec la décision déjà prise pour la signature dans le correctif précédent.
- **Mentions travail/conduite restent manuelles** (encouragements/avertissement/blâme) ; seul le tableau d'honneur devient automatique, conformément à la règle stricte donnée par l'utilisateur ("tous et uniquement les élèves ayant une moyenne ≥ 12").
- **Pas de nouvelle vue "étudiants par spécialité"** : la structure Filière → Niveau → Classe déjà en place sert déjà cet objectif ; seul le chef de département manquait.

## Correctif — Encaissement flexible, reçu, retards justifiés, bug secrétaire, jeu de données démo
- Statut : **terminé**

### Contexte
Retour utilisateur en plusieurs points distincts : (1) le reçu doit afficher le filigrane logo comme les autres documents et le solde restant de la **pension totale** (pas de la seule tranche), en rouge ; (2) le modèle d'encaissement était trop rigide (un paiement = une tranche précise, plafonné à son solde) — un caissier doit pouvoir encaisser n'importe quel montant, qui peut couvrir plusieurs tranches d'un coup ou une seule partiellement, tant que le total ne dépasse pas la pension ; (3) les retards doivent avoir, comme les absences, une notion de "non justifié" ; (4) bug signalé : le menu Bulletins de la secrétaire n'affichait aucune séquence dans la liste déroulante, alors qu'elles apparaissent chez l'admin ; (5) demande d'un jeu de données de démonstration plus riche (2 apprenants et 8 matières par type d'établissement), notes université volontairement non saisies (l'utilisateur les saisira lui-même).

Une plainte préalable sur ce même fil ("signature/logo absents des documents") avait été investiguée et corrigée séparément (commit `584d351`, avant le présent correctif) : il s'agissait de documents déjà émis avant le téléversement du logo/de la signature (les documents PDF sont des instantanés figés au moment de leur génération, jamais régénérés rétroactivement, cf. correctifs précédents) — pas un défaut de code. Un vrai bug d'encodage a cependant été trouvé et corrigé au passage : le caractère Unicode `≥` dans "Tableau d'honneur (moyenne ≥ 12)" sur `bulletin.blade.php` s'affichait comme "moyenne ? 12" (dompdf ne le rend pas avec la police par défaut) — remplacé par la notation ASCII ">=".

### Réalisé
- **Refonte de l'encaissement (changement le plus important)** : un paiement n'est plus rattaché à une tranche précise. Nouveau `EcheancierService` (`calculerTranches()`, `soldeTotalPension()`, `totalPaye()`) qui centralise un calcul **en cascade** : le total payé sur l'inscription est réparti sur les tranches triées par numéro, chacune soldée avant que le reliquat ne s'applique à la suivante — exactement le comportement demandé. `paiements.tranche_id` devient nullable (conservé uniquement pour l'affichage historique des anciens paiements) ; `PaiementService::encaisser()` ne prend plus que `inscription_id`/`montant`/`mode_paiement`, rejette tout montant dépassant le solde total de la pension. `ApprenantController::echeancier` et `RapportService::listerImpayes()` délèguent désormais entièrement à `EcheancierService` (même résultat, calcul unifié en un seul endroit).
- **Reçu** : solde affiché désormais au niveau de la **pension totale** (plus de ligne "Tranche"), en rouge (`#dc2626`) quand positif ; filigrane logo déjà présent en code, revérifié visuellement après génération d'un reçu réel avec un logo effectivement téléversé.
- **Frontend Caisse** : `CaissePage`/`PaiementFormModal` n'ont plus de bouton "Encaisser" par tranche — un seul bouton "Encaisser un paiement" par inscription ouvre une modale demandant un montant (plafonné visuellement au solde de la pension) ; le tableau des tranches reste affiché en lecture seule (échéances/statuts).
- **Conduite — retards justifiés** : `retards_non_justifies` ajouté sur `conduites_releves` et `bulletins` (miroir exact d'`absences_non_justifiees`), colonne "Dont NJ" ajoutée à `SaisieConduitePage`, copié à la clôture de séquence comme les autres champs de conduite, affiché sur le bulletin ("Retards : X (dont Y non justifiés)").
- **Bug secrétaire/séquences** : `SequencePolicy`/`SemestrePolicy::viewAny()` n'incluaient pas `secretaire` (3ᵉ occurrence de ce même type de bug dans le projet — policy avec liste de rôles codée en dur incomplète, déjà rencontré sur `ApprenantPolicy` puis `ClassePolicy`/`FraisScolaritePolicy`) ; route `GET /sequences`/`GET /semestres` séparée du groupe `enseignant`-only pour inclure `secretaire` (sans donner accès aux affectations/notes, hors de son ressort).
- **Jeu de données démo** : chaque type d'établissement (primaire, secondaire, universitaire, centre de formation) a désormais **8 matières par niveau** et **2 apprenants** inscrits dans une même classe (le Lycée gardait déjà Aïcha/Moussa dans des classes différentes, conservés tels quels) ; un compte enseignant de démo créé pour les 3 établissements qui n'en avaient pas (primaire, université, centre de formation), avec une affectation sur chacune des 8 matières de leur classe de démo — **aucune `Note` n'est créée pour l'université**, conformément à la demande explicite.
- Tests Feature : `PaiementTest` largement réécrit (paiement unique couvrant deux tranches d'un coup avec cascade vérifiée, rejet si le montant dépasse la pension totale, rejet si la pension est déjà intégralement soldée), `ConduiteReleveTest`/`BulletinTest` étendus pour `retards_non_justifies`, `SequenceTest`/`SemestreTest` : nouveau test de régression pour la secrétaire — 148/148 tests OK au total (suite complète).
- Vérification (pas de navigateur disponible dans cet environnement — vérifié directement via les services/API et rendu visuel des PDF) : `migrate:fresh --seed` propre, encaissement réel de 70 000 sur une pension à 3 tranches de 50 000 (cascade confirmée : tranche 1 soldée, tranche 2 partiellement couverte), reçu généré avec un logo réellement téléversé rendu visuellement (filigrane + en-tête + solde pension en rouge confirmés sur le PDF), comptage en base des 4 établissements (2 apprenants / 8 matières par niveau chacun, 0 note pour l'université).

### Bugs corrigés pendant cette vérification
- **`PaiementResource::whenLoaded('tranche', ...)` aurait planté** dès qu'un paiement avec `tranche_id = null` était chargé avec la relation `tranche` : `whenLoaded()` teste seulement si la relation a été *interrogée*, pas si elle a résolu une valeur non nulle — la closure était donc quand même invoquée sur un `tranche` valant `null` ("Attempt to read property 'id' on null"). Corrigé par une garde explicite (`$this->tranche ? [...] : null`) avant même qu'un test ne le révèle.
- **Régression évitée dans `RapportService::listerImpayes()`** : un filtre naïf sur `statut === 'en_retard'` aurait silencieusement exclu du rapport les tranches partiellement payées mais en retard (mon calcul cascade classe celles-ci en `partielle`, pas `en_retard`) — corrigé en revenant à la condition d'origine (`solde > 0 && échéance dépassée`), qui ne dépend pas du libellé de statut dérivé.
- **Faux positif pendant la vérification manuelle du filigrane** : un premier test avec un PNG 1×1 fabriqué à la main en base64 ne s'affichait pas du tout dans le PDF rendu, laissant croire à un bug de rendu dompdf (`position: fixed` + `z-index: -1`) — un second test avec un PNG généré proprement (GD) a confirmé que le code de filigrane fonctionne correctement ; le premier fichier de test était simplement invalide/trop minimal pour le décodeur d'image de dompdf, pas un défaut du code applicatif.

### Hypothèses / écarts documentés
- **Migration `create_paiements_table` éditée directement** (plutôt qu'une migration `ALTER` séparée) pour rendre `tranche_id` nullable : exception ponctuelle assumée, ce projet n'ayant jamais été déployé avec de vraies données de production (chaque itération se termine par `migrate:fresh --seed`) et `doctrine/dbal` (requis par `Schema::table()->change()`) n'étant pas installé. Reste une exception : la convention générale du projet (ne jamais éditer une migration déjà mergée) continue de s'appliquer partout ailleurs.
- **Aucune vérification navigateur (Playwright) possible dans cet environnement** pour ce correctif (pas d'outil de pilotage navigateur disponible) : la vérification a porté sur les services/API directement (via `php artisan tinker`) et le rendu visuel réel des PDF générés (technique PyMuPDF déjà utilisée dans les correctifs précédents), plus la suite de tests automatisés complète. Le rendu React (`CaissePage`/`PaiementFormModal`/`SaisieConduitePage`) n'a donc pas été observé dans un vrai navigateur pour ce tour — à faire à la prochaine session si un outil de navigateur devient disponible.

## Correctif — Départements universitaires, UE/EC par semestre, rattrapage, relevé annuel LMD, verrouillage séquentiel, PV, menus par type d'établissement
- Statut : **terminé**

### Contexte
Le modèle académique universitaire ne collait plus à la réalité du terrain : `Filiere` portait directement un chef, les matières (EC) étaient partagées sur tout le niveau (donc dupliquées entre les 2 semestres), aucun regroupement en Unités d'Enseignement (UE) n'existait, aucune notion de rattrapage, et le relevé LMD se générait un semestre à la fois. L'utilisateur a fourni un vrai relevé universitaire (`releve_de_note_universitaire.jpeg`) comme référence : département → filière (spécialité) → semestre → UE (typée fondamentale/professionnelle/transversale, crédits ECTS) → EC, avec sessions d'examen, rattrapage et signature du chef de département. Demande complémentaire : masquer les menus non pertinents selon le type d'établissement (ex. "Séquences" n'a pas de sens pour une université), et permettre à l'enseignant d'imprimer un PV des notes par séquence/semestre.

Décisions validées avec l'utilisateur (AskUserQuestion) avant l'implémentation :
- **Pondération UE → moyenne de semestre** : pondérée par crédits ECTS (le type d'UE reste une étiquette d'affichage, pas un pourcentage fixe à paramétrer).
- **Mention par EC** : notation par lettres (A/B+/B/B-/C+/C.../F), réutilisant les **mêmes seuils** que le barème français du bulletin secondaire (`BulletinService::appreciation()`), simplement relabellisés.
- **Rattrapage** : remplace uniquement la composante Session Normale dans le calcul CC/SN déjà pondéré par matière, et n'est saisissable que si l'EC n'est pas validé (moyenne CC/SN < 10).

### Réalisé
- **Départements** : nouveau modèle `Departement` (nom, code, chef = un `enseignant` du même établissement) ; `Filiere.chef_departement_id` retiré au profit de `Filiere.departement_id` (obligatoire pour une filière universitaire, interdit sinon). CRUD complet (backend + nouvelle page `features/parametrage/departements/`).
- **UE (Unité d'Enseignement) et EC scopés par semestre** : nouveau modèle `UniteEnseignement` (code, nom, type fondamentale/professionnelle/transversale, rattachée à un semestre précis) ; `Matiere` gagne `semestre_id`/`unite_enseignement_id`/`code`, obligatoires pour un niveau LMD (validés croisés : le semestre doit appartenir au niveau, l'UE au semestre), toujours `null` pour le système classique. Nouvelle page `features/parametrage/unites-enseignement/` ; `MatiereFormModal` gagne les champs conditionnels correspondants.
- **Rattrapage SN** : `Note.type_evaluation` gagne la valeur `rattrapage` (migration `->change()` sur l'ENUM, cf. bug ci-dessous) ; `NoteService::soumettre()` rejette un rattrapage si CC/SN ne sont pas verrouillés ou si l'EC est déjà validé (moyenne ≥ 10) ; le calcul du relevé recalcule la moyenne de l'EC avec le rattrapage à la place de la SN uniquement quand la moyenne de base est < 10.
- **Relevé annuel LMD combiné** : `ReleveService::genererAnnuelLmd()` remplace la génération semestre par semestre — un seul document par apprenant couvrant les 2 semestres, groupé Semestre → UE → EC, crédits acquis par UE (compensables au sein de l'UE), moyenne de semestre et annuelle pondérées par crédits ECTS. `releve.blade.php` refondu (typographie compacte, tient sur **une seule page**, vérifié par rendu PyMuPDF), signature "Chef de département" en plus du directeur. `RelevesPage.jsx` simplifiée : un seul bouton "Générer le relevé annuel" pour les deux systèmes.
- **Verrouillage séquentiel** : `NoteService::soumettre()` bloque la saisie d'une séquence/semestre de numéro > 1 tant que la période précédente n'est pas complète (CC+SN verrouillés pour toutes les EC de la classe en LMD, note de séquence verrouillée en classique) — même idiome de message que les autres clôtures du projet.
- **PV (procès-verbal) imprimable** : nouveau `PvService`, généré à la volée et **non persisté/non numéroté** (même logique que `RapportService` — document de travail, pas un document officiel) ; bouton "Imprimer le PV" sur `SaisieNotesPage` une fois une période sélectionnée.
- **Masquage des menus par type d'établissement** : `UserResource` expose désormais `etablissement.type_etablissement` ; `navigation.js` accepte un `etablissementTypes` optionnel par entrée (Séquences → classique uniquement, Semestres/Départements/Unités d'enseignement → universitaire uniquement, Saisie de la conduite/Bulletins → classique uniquement, cohérent avec les règles métier déjà établies).
- **Jeu de données démo** : Université Démo DJAART restructurée avec un département "GTIC" (chef = l'enseignant de démo) portant 2 filières — Génie Logiciel (pleinement peuplée : 2 semestres, 3 UE par semestre d'un type différent chacune, 2 EC par UE, 2 apprenants) et Maintenance des Systèmes Informatiques (coquille minimale, démontre juste le regroupement par département). Réduction assumée du nombre d'EC par rapport au correctif précédent (désormais réparties par semestre avec regroupement UE) — nécessaire pour que le relevé annuel tienne sur une page.
- Tests Feature : `DepartementTest` (via `AcademicStructureTest` étendu), validations `Filiere.departement_id`, `NoteTest` étendu (rattrapage accepté/rejeté selon validation, verrouillage séquentiel bloqué puis débloqué), `ReleveTest` réécrit pour le relevé annuel LMD combiné — 154/154 tests OK au total (suite complète).
- Vérification directe (`php artisan tinker` + rendu PyMuPDF, aucun outil de navigateur disponible dans cet environnement) : structure département/filières/UE/EC confirmée en base ; verrouillage séquentiel testé (rejet puis acceptation) ; rattrapage testé (rejet sur EC déjà validé, acceptation sur EC non validé) ; relevé annuel LMD généré et rendu visuellement — **une seule page**, groupes UE/EC corrects, ligne de rattrapage recalculée correctement, mention "Admis", signature du chef de département ; PV généré et rendu ; relevé annuel **classique** re-vérifié (même template partagé) pour confirmer l'absence de régression.

### Bugs trouvés et corrigés pendant cette vérification
- **`notes.type_evaluation` est un vrai ENUM MySQL** (pas seulement une validation applicative comme supposé initialement) : ajouter `'rattrapage'` sans migration faisait échouer l'insertion (`Data truncated for column 'type_evaluation'`) dès la première soumission réelle. Corrigé par une migration `Schema::table('notes', fn($t) => $t->enum(...)->change())` — vérifié empiriquement que Laravel 12 sait désormais altérer un ENUM nativement **sans** `doctrine/dbal` (non installé dans ce projet), sur MySQL **et** SQLite (contrairement au précédent correctif ENUM qui ne touchait que MySQL, celui-ci devait aussi fonctionner sous SQLite car il ajoute une valeur réellement exercée par les tests, pas seulement en retire une).
- **Verrouillage séquentiel : mauvais périmètre de vérification** — la vérification de complétude de la période précédente regardait initialement *toutes* les affectations de la classe, alors qu'en LMD chaque EC n'appartient qu'à un seul semestre ; un EC du Semestre 2 était donc à tort signalé "incomplet au Semestre 1" (aucune note n'y ayant jamais été saisie), bloquant à tort la saisie du Semestre 2 même une fois le Semestre 1 réellement complet. Détecté par le test `test_semestre_2_accepte_une_fois_le_semestre_1_complet` ajouté pour cette fonctionnalité ; corrigé en filtrant les affectations par `matiere->semestre_id` avant de vérifier leur complétude.

### Hypothèses / écarts documentés
- **Crédits ECTS non stockés sur `UniteEnseignement`** : dérivés à la volée (somme des crédits de ses EC), même principe que le solde de tranche déjà calculé dynamiquement (`EcheancierService`) — évite tout risque de désynchronisation.
- **Mention par EC en lettres, mais mention de semestre/annuelle en `Admis`/`Ajourné`** (inchangé) : la lettre reste réservée au détail par EC, cohérent avec la décision validée par l'utilisateur de ne pas introduire une échelle entièrement nouvelle au niveau synthétique.
- **PV non numéroté/non persisté**, à la différence des bulletins/relevés/reçus — c'est un document de travail pour l'enseignant, pas un document officiel de l'établissement.
- **Aucun outil de navigateur disponible dans cet environnement** (déjà constaté au correctif précédent) : la vérification a porté sur les services/API directement et le rendu visuel réel des PDF générés, plus la suite de tests automatisés complète.

## Correctif — Séance photo (caméra + import en masse), refonte du système de design, landing page
- Statut : **terminé**

### Contexte
Trois demandes combinées : (1) jusqu'ici la photo d'un apprenant ne pouvait être téléversée qu'une par une, manuellement — l'utilisateur veut une véritable **séance photo de classe** : prise de vue directe via la caméra du navigateur (caméra arrière sur mobile), une liste de classe imprimable pour suivre l'ordre, et un import en masse d'un dossier de photos déjà prises, attribuées automatiquement par ordre alphabétique après vérification que les comptes correspondent ; (2) le système de design était minimal (5 couleurs plates, aucun token d'ombre/rayon) et **la navigation mobile était purement absente** (`Sidebar` `hidden md:block` sans aucun repli) — décision validée avec l'utilisateur (AskUserQuestion) : refonte du système de design partagé plutôt qu'une reprise page par page, l'effet se propageant automatiquement via les composants déjà communs à la quasi-totalité des pages ; (3) il n'existait **aucune page publique** (`/` redirigeait directement vers `/dashboard` puis `/login`) — l'utilisateur veut une vraie landing page marketing avec guide de démarrage, fonctionnalités clés, tarifs en FCFA (décision validée : 3 formules par nombre d'apprenants) et CTA "Demander une démo gratuite".

### Réalisé
- **Séance photo — caméra** : nouvelle page `SeancePhotoPage.jsx`, sélection d'une classe puis capture via `navigator.mediaDevices.getUserMedia({ video: { facingMode: { ideal: 'environment' } } })` (préférence caméra arrière), capture apprenant par apprenant dans l'ordre alphabétique avec compteur de progression, upload immédiat via l'endpoint existant (`POST /api/apprenants/{id}/photo`, inchangé).
- **Liste de classe** : nouveau `PhotoSeanceService::genererListePdf()` (génération à la volée, non persistée, même logique que les rapports), colonnes Ordre/Nom/Prénom/Né(e) le/Matricule triées dans le même ordre que l'import en masse — feuille de suivi pour le photographe.
- **Import en masse** : sélection d'un dossier ou de plusieurs fichiers, tri naturel côté client (`IMG_2.jpg` avant `IMG_10.jpg`), vérification bloquante que le nombre de photos égale l'effectif actif de la classe, aperçu (vignette + apprenant assigné) avant confirmation. Nouveau `PhotoMasseController` : le mapping se fait **uniquement par position** (`photos[i]` ↔ effectif recalculé côté serveur, trié par nom/prénom, à l'index `i`) — jamais par un identifiant envoyé par le client, pour éviter tout risque d'attribution manipulée.
- **Refonte du système de design** : palette de marque étendue (teintes claires dérivées de navy/blue/orange/teal, tokens d'ombre `shadow-soft`/`shadow-brand`/`shadow-glow-orange`) dans `index.css` ; **navigation mobile ajoutée** — `Sidebar` devient un tiroir coulissant avec overlay, piloté par un bouton hamburger dans `Topbar`, se referme automatiquement au changement de route ; `Button` (tailles, variant `outline`), `Modal` (transitions, ancré en bas sur mobile), `Table` (en-tête teinté), `Toast` (transitions, bouton de fermeture, variant `warning`) affinés ; nouveau composant `Spinner`. Pages d'authentification refondues en écran partagé (bandeau de marque à gauche, formulaire à droite), via un nouveau composant partagé `AuthLayout`.
- **Landing page** : nouvelle route publique `/` (`LandingPage.jsx`, hors `ProtectedRoute`), sections hero, "Comment commencer" (guide descriptif en 4 étapes, la création d'établissement restant réservée au `super_admin` comme aujourd'hui — le CTA "demander une démo" indique un modèle lead-gen B2B, pas une inscription libre en self-service), fonctionnalités clés (ancrées dans les fonctionnalités réellement implémentées), tarifs (3 formules par nombre d'apprenants, montants indicatifs en FCFA), CTA final. Formulaire "Demander une démo" → nouveau modèle `DemandeDemo` (table hors multi-tenant, aucun établissement n'existe encore pour ces contacts), route publique throttlée, page `DemandesDemoPage.jsx` réservée au `super_admin` pour consulter les prospects.
- Tests Feature : `PhotoMasseTest` (rejet si comptage différent, mapping par position vérifié explicitement, isolation multi-établissement), `DemandeDemoTest` (création publique, validation, liste réservée super_admin) — 162/162 tests OK au total (suite complète).
- Vérification (aucun outil de navigateur disponible dans cet environnement, comme les deux correctifs précédents) : suite de tests complète, `npm run build`/`npm run lint` sans erreur, génération réelle d'une liste de classe rendue visuellement via PyMuPDF, endpoint public de demande de démo testé en direct sur le serveur de développement (réponse 201 correcte), routes `/` et `/apprenants/seance-photo` confirmées servies sans erreur par le serveur de développement.

### Hypothèses / écarts documentés
- **Pas de nouvelle police web** : le caractère "original" du design repose sur la palette élargie, les ombres/transitions et une échelle typographique plus affirmée sur la police système déjà en place — évite l'auto-hébergement/licence d'une police externe pour un gain marginal.
- **Refonte par système de design, pas page par page** : quelques pages représentatives (Caisse) ont reçu un passage ponctuel de finition responsive ; la reprise visuelle des ~40 pages restantes n'a pas été faite individuellement, cohérent avec la décision validée par l'utilisateur.
- **Pas de mécanisme pour installer un "skill de design" tiers** dans cet environnement — la refonte a été réalisée directement (palette/typo/composants/mise en page).
- **Demande de démo = capture de prospect, pas une inscription libre en self-service** : la création d'un établissement reste une action réservée au `super_admin` (`EtablissementPolicy::create`, inchangé) ; aucune intégration e-mail n'a été branchée sur la soumission du formulaire (liste consultable par le super_admin uniquement, à ce stade).
- **Sélection de dossier (`webkitdirectory`) non supportée par Firefox** : un bouton de repli "sélectionner plusieurs fichiers" est proposé à côté, fonctionnel sur tous les navigateurs.

## Correctif — Redirection auto de la page publique, design de la page de connexion, bouton CTA invisible
- Statut : **terminé**

### Contexte
Trois bugs signalés après la mise en ligne de la landing page : (1) la page publique `/` redirigeait automatiquement vers `/login` moins d'une seconde après son ouverture ; (2) le panneau de marque de la page de connexion s'affichait délavé/blanc sur desktop, texte à peine visible ; (3) sur la landing page, le bouton "Découvrir les fonctionnalités" était invisible (texte blanc sur fond blanc), visible seulement au survol.

### Réalisé
- **Redirection automatique** : l'intercepteur Axios de `axiosClient.js` redirigeait vers `/login` sur **toute** réponse 401, y compris l'appel silencieux de vérification de session que `AuthContext` déclenche même sur la landing page. Corrigé en excluant les pages publiques (`/`, `/login`, `/forgot-password`, `/reset-password`) de cette redirection.
- **Panneau de marque délavé** : collision sur la propriété CSS `background-image` entre la classe `.brand-mesh` (dégradé + motif de points) et l'utilitaire Tailwind `bg-gradient-to-br` appliqué en plus sur le même élément — deux règles ciblant la même propriété ne fusionnent pas, celle arrivant en dernier dans la feuille compilée gagne entièrement, sans rapport avec l'ordre des classes dans le JSX. Corrigé en réunissant motif + dégradé dans une seule déclaration `background-image` sur `.brand-mesh`, et en retirant l'utilitaire Tailwind redondant sur `AuthLayout.jsx` et sur le hero de `LandingPage.jsx` (même collision).
- **Bouton CTA invisible** : même famille de bug — `variant="outline"` de `Button.jsx` fixe `bg-white`/`text-brand-blue` en dur, et la tentative de forcer `text-white`/`border-white` via une classe additionnelle perdait le bras de fer de spécificité Tailwind selon l'ordre de compilation. Corrigé en ajoutant un variant dédié `outlineOnDark` (transparent, bordure/texte blancs, conçu pour un fond sombre) plutôt que de continuer à surcharger `outline` au cas par cas.
- Vérification (aucun outil de navigateur disponible dans cet environnement, comme les correctifs précédents) : `npm run build` sans erreur après chaque correctif ; les seuls autres usages de `variant="outline"` du projet (`ImportPhotosMode.jsx`, `SeancePhotoPage.jsx`) vérifiés par grep — sur fond clair, non concernés par ce bug.

### Hypothèses / écarts documentés
- **Leçon générale retenue** : une collision de propriété CSS entre une classe utilitaire et une classe personnalisée ciblant la même propriété (`background-image` ici) ne se résout jamais par l'ordre des classes dans `className` — seul l'ordre de la feuille compilée compte. À chaque futur bug de ce type, vérifier en premier si deux règles se disputent la **même propriété** avant de chercher ailleurs.

## Correctif — Droits d'accès supplémentaires par utilisateur, au-delà des rôles fixes
- Statut : **terminé**

### Contexte
Demande utilisateur : en plus des 6 rôles fixes existants (dont les privilèges sont déjà connus), permettre à un `admin_etablissement` d'accorder individuellement un droit d'accès supplémentaire (à une fonctionnalité précise) à n'importe quel autre acteur de son établissement — l'acteur voit alors apparaître les menus supplémentaires correspondants sur son tableau de bord.

Décisions validées avec l'utilisateur (AskUserQuestion) : granularité **par écran/menu précis** (pas par grand domaine Finance/Pédagogie/Paramétrage) ; **"Comptes utilisateurs" et "Demandes de démo" restent définitivement hors catalogue**, jamais délégables (le premier pour éviter un chemin d'auto-élévation de privilèges, le second réservé au `super_admin` que l'`admin_etablissement` lui-même n'a pas).

### Réalisé
- **Catalogue de 15 droits** (`app/Support/GrantablePermissions.php`, clés `acces.xxx`), seedé via un nouveau `PermissionSeeder` (table `permissions` de Spatie, jusqu'ici posée en Phase 0 mais jamais peuplée) — aucune nouvelle dépendance : `User` utilise déjà `HasRoles`, qui inclut `HasPermissions` de Spatie.
- **`routes/api.php`** : chaque groupe de routes concerné passe de `role:...` à `role_or_permission:...|acces.xxx` (middleware Spatie déjà enregistré en Phase 1 mais jamais utilisé jusqu'ici) — un seul type de garde suffit, Spatie interprétant chaque segment comme "rôle **ou** permission portant ce nom". Les routes de lecture partagées entre plusieurs écrans (ex. `GET /classes`, `GET /sequences`) voient leur liste élargie à l'union des permissions de tous les écrans qui en dépendent. `/users` et `/demandes-demo` restent inchangés (`role:` pur, jamais délégables).
- **Policies** : chaque `viewAny`/`create` concerné devient `hasAnyRole([...]) || $user->can('acces.xxx')`. Les vérifications de propriété fine déjà en place (ex. `AffectationEnseignantPolicy::soumettreNotes` comparant `enseignant_id === user->id`, `deverrouillerNotes` réservé aux admins) restent **inchangées** — un droit accordé ouvre l'accès à l'écran, jamais une bascule de propriété. Même traitement pour 3 contrôleurs à vérification de rôle **inline** (pas via Policy) découverts au passage : `PhotoSeanceController`/`PhotoMasseController` (`acces.seance_photo`), `ConduiteController` (`acces.conduite`).
- **`PUT /api/users/{user}/permissions`** (`UserController::updatePermissions`) : valide chaque permission via `Rule::in(GrantablePermissions::cles())`, vérifie que la cible appartient au même établissement que l'acteur (sauf `super_admin`), puis `syncPermissions()`. `UserResource` expose désormais `permissions` (`getDirectPermissions()->pluck('name')`), qui remonte automatiquement jusqu'à `useAuth().user.permissions` côté frontend sans transformation intermédiaire.
- **Frontend** : `config/navigation.js` (chaque entrée délégable gagne une clé `permission`, un groupe reste visible dès qu'au moins un enfant est autorisé par rôle **ou** permission) ; `ProtectedRoute.jsx` gagne une prop `permission` optionnelle ; `AppRoutes.jsx` éclate les anciens blocs `<Route>` groupés (ex. le grand bloc Paramétrage) en un wrapper par écran délégable ; nouveau `config/permissionsCatalog.js` (miroir du catalogue backend, groupé par domaine pour l'affichage) et `UserPermissionsModal.jsx` (liste à cocher), nouveau bouton "Droits d'accès" sur `UsersListPage.jsx`.
- Tests Feature : nouveau `UserPermissionsTest` (secrétaire bloquée avant droit accordé, `admin_etablissement` accorde `acces.caisse` → la secrétaire peut désormais encaisser, retrait du droit qui bloque à nouveau l'accès, nom de permission hors catalogue rejeté, isolation multi-établissement) — 167/167 tests OK au total (suite complète).
- Vérification : `migrate:fresh --seed` propre (15 permissions en base) ; via `php artisan tinker`, `syncPermissions(['acces.caisse'])` sur une secrétaire de démo confirmé, `UserResource` renvoyant bien `permissions: ["acces.caisse"]` (donc `GET /api/me` aussi) ; `npm run build`/`npm run lint` sans erreur.

### Bug découvert et corrigé pendant l'implémentation
- **Premier passage incomplet, détecté par le test `UserPermissionsTest` lui-même** : accorder `acces.caisse` à une secrétaire ne suffisait pas à la laisser encaisser (`POST /api/paiements` restait 403) alors que le middleware de route avait bien été mis à jour. Cause : chaque `Store*Request`/`Update*Request` du projet définit sa propre méthode `authorize()` qui appelle `$this->user()->can('create'/'update', ...)` — un point d'autorisation **distinct** du contrôleur, invisible à une recherche limitée aux contrôleurs (`$this->authorize(...)`). Corrigé en étendant également les méthodes `create()` de 12 Policies (`Paiement`, `Filiere`, `AnneeAcademique`, `Inscription`, `FraisScolarite`, `Classe`, `Departement`, `Sequence`, `Matiere`, `UniteEnseignement`, `Niveau`, `Semestre`, `AffectationEnseignant`) avec le même `|| $user->can('acces.xxx')`. Leçon générale : dans ce projet, un même écran peut être gardé à **deux endroits distincts** (middleware de route + `authorize()` d'un Form Request) — toujours vérifier les deux avant de conclure qu'une Policy est complète.

### Hypothèses / écarts documentés
- **Granularité par écran précis** (15 permissions) plutôt que par grand domaine — accepté un mapping plus long entre routes/permissions, mais un modèle mental simple pour l'admin qui accorde les droits ("je veux qu'il voie cet écran-là précisément").
- **"Comptes utilisateurs" et "Demandes de démo" ne seront jamais ajoutés au catalogue** — décision de sécurité durable, pas une simplification temporaire.
- **Aucun outil de navigateur disponible dans cet environnement** (déjà constaté aux correctifs précédents) : vérification par tests automatisés + `tinker` + `build`/`lint`, pas de parcours réel en navigateur pour ce tour.

## Correctif — Écrans annexes cassés par un droit d'accès accordé isolément (formulaires dépendant de listes non couvertes)
- Statut : **terminé**

### Contexte
Retour utilisateur : après avoir accordé le droit `acces.affectations` à un compte `enseignant`, le formulaire "Nouvelle affectation" ne se chargeait pas sur son tableau de bord.

### Réalisé
- **Cause racine** : le mapping initial des permissions (correctif précédent) ne couvrait que la route **principale** de chaque écran délégable, pas les appels de **lecture annexes** que leurs formulaires font pour peupler des listes déroulantes. `AffectationFormModal` charge en parallèle `GET /classes`, `GET /matieres` et la liste des enseignants (`GET /users?role=enseignant`) — seule la première avait été couverte par la révision de permissions ; les deux autres restaient verrouillées aux rôles admin, provoquant un 403 sur l'un des trois appels `Promise.all` et bloquant tout le formulaire.
- **`GET /users` ne pouvait pas simplement être élargi** : ce serait revenu sur la décision de sécurité du correctif précédent ("Comptes utilisateurs" jamais délégable, pour éviter qu'un droit accordé sur un écran quelconque ne permette de lister tous les comptes de l'établissement). Nouveau point d'entrée dédié et minimal `GET /affectations/enseignants` (`AffectationController::enseignants()`) : ne renvoie que `id`/`name`/`email` des enseignants de l'établissement, gardé par `acces.affectations` **ou** `acces.parametrage_academique` (réutilisé aussi par `ClasseFormModal` pour le choix du professeur principal, qui souffrait du même problème).
- **Audit systématique des autres écrans délégables** (plutôt que de corriger au coup par coup) : plusieurs autres formulaires dépendaient de lectures non couvertes par leur propre droit — Séance photo/Documents en masse (`GET /classes`, `GET /inscriptions` pour reconstituer l'effectif d'une classe), Caisse (`GET /apprenants` pour la recherche, `GET /apprenants/{id}/echeancier`), et tous les formulaires de Séquences/Semestres/Frais de scolarité/Affectations qui chargent Filières/Niveaux/Années académiques (jusque-là strictement réservées à `acces.parametrage_academique`).
- **Simplification plutôt que multiplication des permissions** : les lectures de données de **référence non sensibles** (aucune donnée financière ni personnelle) — Classes, Filières, Niveaux, Années académiques, Matières, Départements, Unités d'enseignement, Séquences, Semestres — sont désormais ouvertes à **tout utilisateur authentifié** de l'établissement (`routes/api.php`), plutôt que de continuer à énumérer, écran par écran, quel droit a besoin de quelle liste (risque de répéter ce même bug indéfiniment). Les `Policy::viewAny()` correspondantes deviennent `return true` (la création/modification/suppression reste, elle, strictement gardée). Les lectures réellement sensibles restent gardées précisément : `GET /frais-scolarite` (montants), `GET /apprenants`+`echeancier` (élargi à `acces.inscriptions`/`acces.caisse` en plus d'`acces.apprenants`), `GET /inscriptions` (élargi à `acces.seance_photo`/`acces.documents_masse`).
- Nouveau test de régression `UserPermissionsTest::test_acces_affectations_donne_toutes_les_lectures_necessaires_au_formulaire` : vérifie que les 3 lectures nécessaires au formulaire d'affectation (classes/matières/enseignants) fonctionnent bien une fois le droit accordé — 168/168 tests OK au total (suite complète).
- Vérification directe sur le compte réellement concerné (`php artisan tinker`, `enseignant.universite@djaart.school`, droits `acces.inscriptions`/`acces.seance_photo`/`acces.affectations` réellement accordés par l'utilisateur) : les 5 `Policy::viewAny` dont dépendent ses écrans (Classe, Matiere, AffectationEnseignant, Inscription, Apprenant) renvoient toutes `true`.

### Bug corrigé au passage (régression introduite par ce correctif lui-même, détectée par la suite de tests)
- En retirant `GET /frais-scolarite` de son ancien groupe de rôles lors de la réorganisation des routes, la route avait été supprimée par erreur (405 au lieu de 200) — détecté immédiatement par `FraisScolariteTest::test_comptable_can_list_frais_scolarite`, corrigé en la republiant dans son propre groupe (elle contient des montants, donc volontairement exclue de la liste de lectures ouvertes ci-dessus).

### Hypothèses / écarts documentés
- **Leçon générale retenue** : quand un droit délégable "débloque" un écran, il faut auditer non seulement sa route principale mais **toute la chaîne de lectures que son formulaire déclenche** (`Promise.all` de listes déroulantes) — un seul appel manquant suffit à faire échouer tout le formulaire en silence côté utilisateur.
- **Distinction assumée** : données structurelles de référence (classes, matières, années...) = lecture libre pour tout authentifié de l'établissement ; données personnelles/financières (apprenants, inscriptions, frais de scolarité, comptes utilisateurs) = restent strictement gardées par rôle/permission au cas par cas.

## Correctif — Menus Paramétrage visibles par tous, sidebar en groupes déroulables, notifications par e-mail
- Statut : **terminé**

### Contexte
Deux demandes combinées : (1) un compte `enseignant` (droits `acces.inscriptions`/`acces.seance_photo`/`acces.affectations` accordés) voyait apparaître tout le sous-menu Paramétrage (Établissement(s), Années académiques, Départements, Filières, Classes, Matières, Unités d'enseignement), alors qu'il n'a aucun de ces droits — l'utilisateur a aussi demandé que les grands groupes de menu (Paramétrage/Finance/Pédagogie) deviennent des listes déroulantes plutôt que d'afficher tous leurs enfants en permanence ; (2) mise en place de notifications par e-mail (SMTP Gmail fourni par l'utilisateur) pour que chaque acteur soit informé aux moments clés.

### Réalisé — bug des menus
- **Cause racine** : lors du correctif précédent (droits d'accès supplémentaires), la logique de visibilité d'un groupe de menu était passée de "le groupe entier est verrouillé par le rôle du parent" à "le groupe s'affiche dès qu'un enfant est autorisé (rôle ou permission)". Les 7 sous-entrées de Paramétrage n'avaient jamais eu leur propre champ `roles` (seul le groupe parent le portait) : `allowed()` traite l'absence de `roles` comme "aucune restriction" (`!roles → true`), donc chacune passait pour n'importe quel utilisateur authentifié dès que le verrou du parent a été retiré. Corrigé en réajoutant `roles: PARAMETRAGE_ROLES` sur les 7 entrées, en plus de leur `permission` déjà présente.
- **Sidebar en groupes déroulables** (demande explicite) : `Paramétrage`/`Finance`/`Pédagogie` sont désormais des boutons à bascule (chevron animé) plutôt que des en-têtes toujours dépliés ; le groupe contenant la route actuellement active s'ouvre automatiquement à la navigation.

### Réalisé — notifications par e-mail
- SMTP Gmail réel configuré (`.env`, jamais commité) ; le mot de passe oublié (déjà basé sur les notifications Laravel standard) en bénéficie automatiquement, sans changement de code.
- 6 nouveaux `Mailable` (`app/Mail/`), gabarit HTML partagé (`resources/views/components/emails/layout.blade.php`) :
  - `CompteCreeMail` — à la création d'un utilisateur (`UserController::store`) ; ne transmet jamais le mot de passe en clair, uniquement le lien de connexion.
  - `DroitsAccesModifiesMail` — dès qu'un admin modifie les droits supplémentaires d'un acteur (`UserController::updatePermissions`), liste les droits désormais actifs.
  - `InscriptionValideeMail` — dès que les frais d'inscription sont couverts (transition réelle `en_cours` → `validee` dans `PaiementService`, jamais renvoyé sur les paiements suivants).
  - `PaiementRecuMail` — à chaque encaissement (`RecuService::genererPour`), reçu PDF joint.
  - `BulletinPretMail` / `ReleveDisponibleMail` — à la clôture d'une séquence (`BulletinService`) / génération d'un relevé annuel (`ReleveService`), un e-mail par apprenant disposant d'une adresse.
  - `DemandeDemoRecueMail` — à la soumission du formulaire public de démo, à tous les `super_admin`.
  - Toutes ces notifications sont **conditionnées à la présence d'une adresse e-mail** sur l'`Apprenant` (champ optionnel) — aucun envoi sinon, aucune erreur non plus.
- Tests Feature : nouveau `MailNotificationsTest` (`Mail::fake()` + `assertSent`/`assertNotSent`) couvrant les 6 déclencheurs, dont un cas explicite "apprenant sans e-mail → aucun envoi" — 174/174 tests OK au total (suite complète).
- Vérification directe : envoi réel d'un e-mail de test via `php artisan tinker` avec la configuration SMTP effective (succès confirmé, pas seulement `Mail::fake()`).

### Hypothèses / écarts documentés
- **Envoi synchrone (pas de `ShouldQueue`)** : le projet n'a pas de worker de file d'attente garanti actif en permanence dans cet environnement (`QUEUE_CONNECTION=database`) ; envoyer directement évite qu'un e-mail reste bloqué indéfiniment dans la table `jobs` faute de `php artisan queue:work` en cours d'exécution. À reconsidérer si le volume d'e-mails devient important (bulletins/relevés en masse pourraient ralentir la requête HTTP qui les déclenche).
- **`CompteCreeMail` ne contient jamais le mot de passe en clair** — écart assumé par rapport à une lecture littérale d'"informer à chaque moment crucial" : le mot de passe est déjà communiqué par l'admin qui crée le compte (hors bande), l'e-mail ne sert qu'à confirmer la création et donner le lien de connexion.
- **Aucun outil de navigateur disponible dans cet environnement** (déjà constaté aux correctifs précédents) : vérification par tests automatisés (`Mail::fake()`) + un envoi réel isolé via `tinker`, pas de parcours complet en navigateur pour ce tour.

## Correctif — Commande en ligne (SaaS self-service), multi-établissement par admin, bulletins jumelés/annuel détaillé, vidéos de fond
- Statut : **terminé**

### Contexte
Demande combinée : (1) sur la landing page, chaque formule tarifaire doit avoir un CTA "Commander maintenant" (nom/ville/nombre d'apprenants/téléphone/e-mail/nom d'établissement) plutôt que de rediriger vers la démo ; (2) le super_admin valide la commande en choisissant une durée d'accès, les fonctionnalités incluses et le type d'établissement — un même admin pouvant gérer plusieurs établissements et permuter entre eux sur le même dashboard ; (3) le compte admin créé reçoit ses identifiants par e-mail (mot de passe robuste, changement obligatoire à la première connexion) ; (4) un admin_etablissement ne doit jamais pouvoir changer le type de son propre établissement ; (5) pour le secondaire, pouvoir imprimer un bulletin par séquence, deux séquences jumelées sur une page (tableaux distincts), et un bulletin annuel détaillé (toutes les séquences, une page) ; (6) vidéo en fond du hero de la landing page et de la page de connexion.

Décisions validées avec l'utilisateur (AskUserQuestion) : les "fonctionnalités" choisies à la validation réutilisent le catalogue de 15 droits `acces.xxx` déjà existant (pas un nouveau système de modules par établissement) ; le multi-établissement se fait via une table de liaison + sélecteur d'établissement actif dans le dashboard ; le jumelage de séquences suit des paires fixes automatiques (1+2, 3+4...), pas une sélection libre ; le bulletin annuel détaillé est un **nouveau** document, en plus du relevé annuel existant (qui ne montre que la moyenne par séquence, pas le détail par matière).

### Réalisé
- **Commande publique** : nouveau modèle `Commande` (nom, ville, nombre_apprenants, téléphone, e-mail, nom_établissement, statut `en_attente`/`validee`/`rejetee`), `POST /api/commandes` public throttlé (même schéma que `/demandes-demo`), notifie tous les `super_admin` par e-mail (`CommandeRecueMail`). Landing page : les 3 cartes de formules ont désormais un CTA "Commander maintenant" ouvrant `CommandeModal` (au lieu de "Demander une démo").
- **Validation par le super_admin** (`POST /api/commandes/{commande}/valider`, nouvel écran `/commandes`) : choix du type d'établissement, de la durée d'accès en mois (→ `Etablissement.abonnement_expire_le`) et des droits `acces.xxx` inclus. Deux chemins :
  - **Nouvel admin** (e-mail inconnu) : création de l'établissement + d'un compte `admin_etablissement` avec un mot de passe robuste généré (`Str::password(14)`), `must_change_password = true`, droits choisis synchronisés (`syncPermissions`) — identifiants envoyés par e-mail (`CommandeValideeMail`, mot de passe en clair **uniquement** dans cet e-mail précis, changement forcé dès la connexion).
  - **Admin déjà existant** (même e-mail qu'un `admin_etablissement` déjà en base) : le nouvel établissement est simplement rattaché à son compte (aucun nouveau mot de passe, `NouvelEtablissementAjouteMail` sans identifiants).
- **Multi-établissement par admin** : nouvelle table de liaison `etablissement_user` (migration avec rétro-alimentation automatique pour les `admin_etablissement` déjà existants, `users.etablissement_id` reste "l'établissement actif" sans aucun changement de code ailleurs dans l'application — tout le reste du système continue de lire cette colonne exactement comme avant). Nouveau `PUT /api/moi/etablissement-actif` (vérifie l'appartenance à la table de liaison avant de basculer) ; sélecteur d'établissement dans `Topbar.jsx`, affiché uniquement si le compte en gère plus d'un, rechargement complet du dashboard après permutation.
- **Mot de passe provisoire obligatoire** : `users.must_change_password`, nouveau middleware global `EnsureAccountUsable` (alias `compte.actif`, appliqué à tout le groupe authentifié sauf `/logout`, `/me`, `/moi/mot-de-passe`, `/moi/etablissement-actif` — routes qui doivent rester joignables pour se débloquer soi-même) : renvoie 423 tant que le mot de passe n'a pas été changé via `PUT /api/moi/mot-de-passe`. Nouvelle page `/changer-mot-de-passe` (redirection automatique de `ProtectedRoute` si `must_change_password`).
- **Abonnement expiré** : le même middleware `EnsureAccountUsable` bloque (403) l'accès de tout utilisateur (hors `super_admin`) dont l'établissement actif a dépassé `abonnement_expire_le`.
- **Type d'établissement verrouillé** : `UpdateEtablissementRequest` interdit désormais le champ `type_etablissement` à quiconque n'est pas `super_admin` (`Rule::prohibitedIf`), quel que soit son rôle/droit sur l'établissement.
- **Bulletin jumelé** (`GET /api/bulletins/{bulletin}/telecharger-jumele`) : le bulletin demandé + celui de sa séquence "paire" fixe (1+2, 3+4...) pour le même apprenant, sur une seule page en deux tableaux distincts — si la séquence paire n'est pas encore clôturée, son emplacement affiche un message explicite plutôt que d'échouer. Nécessite un nouveau champ persisté `Bulletin.details_lignes` (détail matière par matière, jusque-là calculé à la clôture puis jeté — seuls les sous-totaux `details_groupes` étaient conservés).
- **Bulletin annuel détaillé** (`GET /api/classes/{classe}/bulletin-annuel-detaille`) : un document par classe, une page par apprenant, tableau matrice matière × séquence (note par séquence + moyenne annuelle par matière, reconstruit à partir de `details_lignes` sans recalcul) + moyenne générale annuelle. Généré à la volée, jamais persisté (même logique que `PvService`/`RapportService`) — document **distinct** du relevé annuel existant.
- **Vidéos de fond** : `hero-bg-public.mp4` (hero de la landing page) et `bg-login.mp4` (bandeau de marque de la page de connexion), servies depuis `public/videos/`, `autoplay muted loop playsInline` avec un voile sombre superposé pour garder le texte lisible — remplacent le dégradé `.brand-mesh` (classe CSS retirée, plus aucun consommateur).
- Tests Feature : nouveau `CommandeEtMultiEtablissementTest` (commande publique + notification, validation nouvel admin vs admin existant, commande déjà traitée rejetée, mot de passe provisoire bloquant, abonnement expiré bloquant sauf super_admin, bascule d'établissement autorisée/refusée, verrouillage du type d'établissement), `BulletinTest` étendu (bulletin jumelé avant/après clôture de la séquence paire, bulletin annuel détaillé) — 187/187 tests OK au total (suite complète).
- Vérification : `migrate:fresh --seed` propre ; envoi réel d'un e-mail de test via la configuration SMTP effective (confirmé au correctif précédent, réutilisée ici sans changement) ; `npm run build`/`npm run lint` sans erreur.

### Hypothèses / écarts documentés
- **"Fonctionnalités qu'il aura" reste additif, pas un plafond dur** : le nouveau compte reçoit le rôle `admin_etablissement`, qui donne déjà accès à la quasi-totalité des écrans indépendamment des permissions individuelles (`hasAnyRole(['admin_etablissement', ...])` dans la majorité des Policies) — les droits choisis à la validation s'ajoutent à ce rôle sans le restreindre. Concrètement, cocher un sous-ensemble des 15 droits ne cache aucun écran à cet admin lui-même ; le mécanisme sert surtout de point de départ cohérent s'il délègue ensuite des droits à son propre personnel (secrétaire, comptable...). Plafonner réellement l'accès d'un `admin_etablissement` par la formule choisie exigerait de revoir individuellement les Policies pour ne plus accorder d'accès automatique par rôle sur ces 15 écrans — chantier distinct, plus risqué (impact sur toutes les Policies et la suite de tests existante), non fait ici faute d'une demande explicite en ce sens.
- **`etablissement_id` = établissement actif, pas un établissement fixe** : décision clé pour limiter l'impact du multi-établissement — tout le reste du code (dizaines de contrôleurs/policies) continue de fonctionner sans modification, puisqu'il lit toujours cette même colonne ; seule la table de liaison `etablissement_user` est nouvelle, et seuls `PUT /api/moi/etablissement-actif` et le sélecteur du dashboard en dépendent.
- **Comptes de démonstration non rétro-liés au multi-établissement** : la migration de rétro-alimentation de `etablissement_user` s'exécute avant les seeders lors d'un `migrate:fresh --seed` (aucun `admin_etablissement` n'existe encore à ce moment) — les comptes de démo restent donc à un seul établissement, sans régression puisque rien de leur usage actuel ne dépend de cette table.
- **Bulletin annuel détaillé** suppose que les matières d'une classe restent les mêmes d'une séquence à l'autre (ordre dérivé de la première séquence clôturée disponible) — cohérent avec le fonctionnement actuel de `cloturerSequence` (les affectations de la classe ne changent pas en cours d'année dans ce système).
- **Aucun outil de navigateur disponible dans cet environnement** (déjà constaté aux correctifs précédents) : vérification par tests automatisés + `migrate:fresh --seed` + `build`/`lint`, pas de parcours réel en navigateur pour ce tour.

## Correctif — Auto-modification des droits, établissement à 2 types, section Contact, lisibilité du nom de marque
- Statut : **terminé**

### Contexte
Retour utilisateur après le correctif précédent : (1) un `admin_etablissement` pouvait modifier ses **propres** droits d'accès supplémentaires via `PUT /api/users/{lui-même}/permissions` (faille de contournement, même si son rôle donne déjà un accès quasi total — reste un principe de sécurité à respecter) ; (2) à la validation d'une commande, le super_admin doit pouvoir choisir **jusqu'à 2 types d'établissement** (ex. secondaire + centre de formation) plutôt qu'un seul ; (3) la section "Contact" de la page publique n'affichait qu'une bannière CTA, sans coordonnées ; (4) sur les barres de navigation, le logo seul ne suffisait pas à rendre "DJAART SCHOOL" lisible (le logo est un empilement icône + nom + slogan, illisible une fois réduit à la hauteur d'une barre de nav).

### Réalisé
- **Faille d'auto-modification des droits** : `UserController::updatePermissions` rejette désormais explicitement toute tentative d'un acteur de modifier ses propres droits (`$acteur->is($user)` → 422), quel que soit son rôle (y compris `super_admin`, par principe). Le bouton "Droits d'accès" est aussi masqué sur sa propre ligne dans `UsersListPage.jsx` (défense en profondeur, pas seulement une garde backend).
- **Établissement à 2 types maximum** : nouveau champ `Etablissement.type_etablissement_secondaire` (nullable, optionnel, doit différer du type principal — validé aux deux niveaux, création et modification). `ValiderCommandeModal` gagne un second sélecteur "2e type (optionnel)" qui exclut automatiquement la valeur déjà choisie comme type principal ; même pattern dans `EtablissementFormModal`. `navigation.js`/`Sidebar.jsx` : la visibilité des menus par type d'établissement (`etablissementTypes`) compare désormais contre les **deux** types cumulés de l'utilisateur (un menu s'affiche dès qu'un des deux correspond), au lieu d'un seul type.
- **Bug découvert en creusant ce point** : `EtablissementFormModal.jsx` envoyait toujours `type_etablissement` dans sa requête de modification, même sans changement — depuis le correctif précédent qui interdit ce champ à quiconque n'est pas `super_admin`, **toute modification d'établissement par un admin_etablissement échouait en 422**, y compris pour changer juste le nom ou l'adresse. Corrigé : le formulaire n'inclut désormais les champs de type dans la requête que si l'acteur est `super_admin` (pour les autres, un texte informatif affiche le type actuel en lecture seule).
- **Section Contact enrichie** : `#contact` sur la landing page passe d'une simple bannière à deux colonnes — CTA (démo + lien vers les formules) et un encadré coordonnées (e-mail `contact@djaart.school`, délai de réponse, zone de couverture). Aucun numéro de téléphone ni adresse physique inventés (non fournis) — uniquement des informations réellement disponibles.
- **Lisibilité du nom de marque** : ajout du texte "DJAART SCHOOL" (typographie grasse, "SCHOOL" en orange de marque) à côté du logo dans tous les emplacements où seul le logo apparaissait — en-tête et pied de page de la landing page, `Topbar.jsx` du tableau de bord, panneau de marque et logo mobile de `AuthLayout.jsx` (page de connexion). Le logo garde un `alt=""` (décoratif) dans ces emplacements puisque le texte adjacent porte désormais le nom pour l'accessibilité.
- Tests Feature : nouveaux tests dans `UserPermissionsTest` (auto-modification rejetée) et `CommandeEtMultiEtablissementTest` (validation avec 2 types cumulés, 2e type identique au principal rejeté, modification d'établissement par un admin sans toucher au type toujours acceptée) — 191/191 tests OK au total (suite complète).
- Vérification : `migrate:fresh --seed` propre ; `npm run build`/`npm run lint` sans erreur.

### Hypothèses / écarts documentés
- **Coordonnées de contact limitées à l'e-mail déjà utilisé sur le site** (`contact@djaart.school`) : aucun numéro de téléphone ni adresse physique n'a été fourni par l'utilisateur pour ce projet — plutôt que d'en inventer, la section Contact reste volontairement sobre sur ce point, à compléter par l'utilisateur avec ses vraies coordonnées le moment venu.
- **Aucun outil de navigateur disponible dans cet environnement** (déjà constaté aux correctifs précédents) : vérification par tests automatisés + `build`/`lint`, pas de parcours réel en navigateur pour ce tour.

## Correctif — Auto-modification du rôle, mots de passe générés, profil personnel, grade du signataire, audit sécurité
- Statut : **terminé**

### Contexte
Cinq demandes combinées : (1) un admin ne doit pas pouvoir changer son propre rôle ; (2) un bouton "Donner tous les droits" à la validation d'une commande ; (3) retirer des interfaces les éléments non liés au(x) type(s) d'établissement de l'acteur (pas seulement les masquer dans le menu) ; (4) quand un admin ajoute un acteur, le mot de passe doit être généré automatiquement et envoyé par e-mail, jamais choisi par l'admin ; (5) un profil personnel pour chaque utilisateur (nom complet, photo, civilité, changement de mot de passe) ; (6) un grade/titre du signataire (Le Directeur, La Directrice, Le Fondateur...) saisi avec la signature, affiché sur les documents à la place du texte figé ; (7) un audit sécurité général de l'application.

### Réalisé
- **Auto-modification du rôle bloquée** : `UpdateUserRequest` interdit le champ `role` dès que l'acteur modifie son propre compte (`Rule::prohibitedIf`), même un `super_admin` — même mécanisme que le blocage du type d'établissement du correctif précédent. `UserFormModal.jsx` masque le sélecteur de rôle sur sa propre ligne (affiche le rôle actuel en lecture seule).
- **"Donner tous les droits"** : nouveau bouton dans `ValiderCommandeModal` (sélectionne en un clic tous les droits **pertinents pour le(s) type(s) d'établissement choisis**, pas l'intégralité du catalogue si des droits ne s'appliquent pas) ; même bouton ajouté à `UserPermissionsModal` (délégation classique), filtré par le(s) type(s) de l'établissement de l'acteur cible.
- **Filtrage des interfaces par type d'établissement, au-delà du menu** : `permissionsCatalog.js` gagne des `etablissementTypes` par droit (Séquences=classique, Semestres/Départements/UE=universitaire, Conduite/Bulletins=classique) et une fonction `filtrerCataloguePourTypes()` réutilisée par les deux modales de droits. Surtout : `ProtectedRoute.jsx` gagne une prop `etablissementTypes` qui bloque désormais l'accès **par URL directe**, pas seulement l'affichage dans la Sidebar — un secrétariat d'un établissement universitaire ne peut plus atteindre `/pedagogie/conduite` en tapant l'URL, alors que jusqu'ici seul le menu la masquait. `super_admin` reste exempté (support/dépannage).
- **Mot de passe jamais choisi par l'admin qui crée un compte** : `UserController::store` génère désormais un mot de passe robuste (`Str::password(14)`) et l'envoie par e-mail (`CompteCreeMail`, mis à jour pour l'inclure — écart assumé par rapport à la décision du tout premier correctif "jamais le mot de passe en clair", révisé ici sur demande explicite), `must_change_password = true`. `UserFormModal.jsx` retire le champ mot de passe à la création ; en modification, un admin peut cocher "Réinitialiser le mot de passe" (nouveau mot de passe généré + `MotDePasseReinitialiseMail`), jamais taper un mot de passe à la place de l'acteur.
- **Profil personnel** (`/mon-profil`, accessible à tout rôle authentifié) : nouveaux champs `User.civilite` (M./Mme/Mlle) et `User.photo`, `PUT /api/moi/profil` (nom + civilité) et `POST /api/moi/photo` — chacun n'agit que sur son propre compte (aucun paramètre `{user}` dans ces routes). Réutilise `PUT /api/moi/mot-de-passe` déjà existant pour le changement volontaire. Accès au profil ajouté dans la `Topbar` (photo/initiale + nom, lien cliquable).
- **Grade du signataire** : nouveau `Etablissement.signature_titre`, saisi en même temps que l'image de signature dans `EtablissementBrandingModal` (champ texte avec suggestions : Le Directeur, La Directrice, Le Fondateur, La Fondatrice, Le Proviseur, La Proviseure). Les 4 documents PDF qui affichaient "Le Directeur / La Directrice" en dur (bulletin, relevé, attestation, carte scolaire) utilisent désormais `$etablissement->signature_titre`, avec ce même texte en repli si non renseigné — le reçu garde volontairement "Signature du caissier" (décision déjà actée, document distinct).
- **Audit sécurité** : aucune sortie Blade non échappée (`{!! !!}`) trouvée ; CORS scopé à `FRONTEND_URL` (pas de wildcard) malgré `supports_credentials: true` ; cookies de session `http_only` par défaut, `same_site=lax` ; vérifié qu'aucune route métier n'échappe au nouveau middleware `compte.actif` (`route:list` inspecté) ; vérifié qu'aucun champ sensible (`statut` d'une commande, `etablissement_id` à la création d'un compte) n'est accessible en mass-assignment au-delà de ce que `FormRequest::validated()` autorise explicitivement.
- Tests Feature : nouveaux tests dans `UserManagementTest` (auto-modification du rôle rejetée, mot de passe généré à la création avec vérification du hash reçu par e-mail, réinitialisation par un admin), nouveau `ProfilTest` (nom/civilité, photo, isolation — impossible d'agir sur un tiers, signature avec titre) — 199/199 tests OK au total (suite complète).
- Vérification : `migrate:fresh --seed` propre ; `npm run build`/`npm run lint` sans erreur.

### Hypothèses / écarts documentés
- **`SESSION_SECURE_COOKIE` non forcé** : reste piloté par l'environnement (`.env`), à mettre impérativement à `true` avant tout déploiement en production HTTPS — non modifié ici car l'environnement de développement local tourne en HTTP simple.
- **Le mot de passe redevient visible en clair dans l'e-mail de création/réinitialisation** (écart assumé par rapport à la décision initiale du tout premier correctif e-mail, qui l'excluait délibérément) — changement demandé explicitement cette fois : l'admin ne choisit plus jamais de mot de passe pour un tiers, donc il n'existe plus d'autre canal pour le communiquer que l'e-mail, protégé par le changement obligatoire à la première connexion.
- **Aucune restriction backend des écrans par type d'établissement** (seulement au niveau des interfaces — Sidebar + `ProtectedRoute`) : la structure académique elle-même (`Niveau.type_systeme`) reste le garde-fou côté données ; ajouter une vérification backend supplémentaire par écran serait un chantier distinct, non demandé explicitement ("retire sur les **interfaces** les éléments...").
- **Aucun outil de navigateur disponible dans cet environnement** (déjà constaté aux correctifs précédents) : vérification par tests automatisés + `build`/`lint`, pas de parcours réel en navigateur pour ce tour.

## Correctif — Multi-établissement pour tout acteur (rôles/droits distincts par établissement), accès LAN
- Statut : **terminé**

### Contexte
Deux demandes combinées : (1) le multi-établissement, jusqu'ici réservé à `admin_etablissement` (correctif Commande), doit s'étendre à **tout** acteur (secrétaire, comptable, enseignant...) — un même compte peut intervenir dans 1, 2, 3 établissements ou plus, avec un rôle et des droits `acces.xxx` **propres à chacun**, permutable depuis le tableau de bord sans se reconnecter ; (2) l'application doit être joignable depuis d'autres appareils du même réseau local que le PC de développement (téléphone, tablette...), pas seulement `localhost`.

### Réalisé
- **Rôle/droits par établissement, pas globaux** : Spatie (`HasRoles`) reste par nature global par utilisateur — plutôt que de le remplacer (chantier risqué, toucherait toutes les Policies), la table de liaison `etablissement_user` gagne deux colonnes `role`/`permissions` (JSON) qui deviennent la **source de vérité par établissement** ; le rôle/droits Spatie "en direct" de l'utilisateur ne sont qu'un reflet de l'établissement **actif** du moment, resynchronisés (`syncRoles`/`syncPermissions`) à chaque bascule. Résultat : tout le reste de l'application (Policies, middlewares `role:`/`role_or_permission:`, `hasAnyRole()`) continue de fonctionner sans aucune modification.
- **`ProfilController::basculerEtablissement`** : en plus de changer `users.etablissement_id`, resynchronise désormais le rôle et les droits en direct depuis le pivot de l'établissement ciblé — un même compte peut ainsi être secrétaire dans un établissement et comptable dans un autre, avec des droits `acces.xxx` distincts de part et d'autre.
- **Ajout d'un acteur dont l'e-mail existe déjà ailleurs** : `UserController::store` ne bloque plus (`unique:users,email` retiré de `StoreUserRequest`) — si l'e-mail correspond à un compte existant, il est **rattaché** au nouvel établissement (pivot créé avec le rôle choisi, `NouvelEtablissementAjouteMail` sans nouveau mot de passe) au lieu d'échouer ou de dupliquer le compte ; rejeté (422) si l'acteur intervient déjà dans cet établissement précis. Un utilisateur nouvellement créé reçoit aussi une ligne de pivot pour son établissement d'origine, pour que la bascule fonctionne correctement plus tard s'il rejoint un 2e établissement.
- **Pivot tenu à jour lors des modifications** : `UserController::update` (changement de rôle) et `updatePermissions` (droits délégués) écrivent désormais aussi dans le pivot de l'établissement actif de l'acteur ciblé, en plus du rôle/droits en direct — sans quoi l'ancien rôle/droits réapparaîtraient à la prochaine bascule vers cet établissement. `CommandeController::valider` (admin_etablissement, chemin existant) adapté au même schéma.
- **`UserResource`/`Topbar.jsx`** : `etablissements_geres` inclut désormais le rôle de l'acteur dans chaque établissement (`{id, nom, role}`), affiché dans le sélecteur du tableau de bord (`Nom (rôle)`).
- **Accès LAN** : `vite.config.js` lie le serveur dev sur `0.0.0.0` (`server.host: true`) au lieu de `localhost` seul ; `axiosClient.js` déduit l'URL du backend à partir de l'hôte utilisé pour charger la page (`window.location.hostname`) quand `VITE_API_URL` n'est pas défini, au lieu d'un `http://localhost:8000` figé — le même build fonctionne donc depuis `localhost`, `127.0.0.1` ou l'IP LAN d'un autre appareil. Côté backend : `SANCTUM_STATEFUL_DOMAINS` et le nouveau `CORS_ALLOWED_ORIGINS` (liste séparée par virgules, distinct de `FRONTEND_URL` qui reste unique pour les liens dans les e-mails) acceptent désormais localhost **et** l'IP LAN du PC hôte ; `SESSION_DOMAIN` mis à `null` (au lieu de `localhost` figé) pour que le cookie de session s'associe correctement à l'hôte réellement utilisé, quel qu'il soit.
- Tests Feature : `CommandeEtMultiEtablissementTest` étendu (bascule entre 2 établissements avec rôles et droits différents, dans les deux sens), `UserManagementTest` étendu (rattachement d'un acteur existant à un nouvel établissement sans dupliquer le compte, rejet si déjà présent dans cet établissement, pivot mis à jour lors d'un changement de rôle et lors d'une modification des droits délégués) — 204/204 tests OK au total (suite complète).
- Vérification : `migrate:fresh --seed` propre ; `npm run build`/`npm run lint` sans erreur.

### Hypothèses / écarts documentés
- **IP LAN codée dans le `.env` local du poste de développement uniquement** (`172.20.10.3`, sujette à changement en cas de réattribution DHCP), jamais dans `.env.example` ni un fichier suivi par git — chaque développeur/déploiement ajuste sa propre IP. `.env.example` documente la démarche (commentaire) sans valeur figée.
- **Pas de refonte de Spatie vers un système de rôles réellement multi-tenant** : la solution retenue (pivot comme source de vérité, resynchronisation Spatie à la bascule) est un compromis délibéré pour préserver 100% du code d'autorisation existant (Policies, middlewares) sans le réécrire — un chantier de refonte complète serait plus robuste dans l'absolu mais démesuré par rapport à la demande.
- **Aucun outil de navigateur disponible dans cet environnement** (déjà constaté aux correctifs précédents) : vérification par tests automatisés + `migrate:fresh --seed` + `build`/`lint`, pas de parcours réel en navigateur pour ce tour ni de test réel depuis un second appareil sur le réseau local.

## Correctif — Établissement à 2 types → 2 établissements distincts partageant le même admin
- Statut : **terminé**

### Contexte
Retour utilisateur en testant le multi-établissement : `admin.universite@djaart.school` (établissement à 2 types cumulés, universitaire + centre de formation, depuis un correctif précédent) n'avait pas le sélecteur de bascule dans son tableau de bord malgré "2 établissements". En creusant : (1) le cumul de 2 types sur un **même** établissement (décision d'un correctif précédent) donne accès aux menus des deux types, mais ce n'est pas un 2e établissement — il n'y a donc jamais eu de quoi basculer ; (2) aucun compte de démo (ni aucun utilisateur existant avant le correctif multi-établissement de ce jour) n'avait de ligne dans le pivot `etablissement_user` pour son propre établissement, donc le sélecteur ne pouvait de toute façon jamais s'afficher. Décision validée avec l'utilisateur (AskUserQuestion) : remplacer **systémiquement** le cumul de types par la création de 2 établissements distincts partageant le même admin (bascule via le sélecteur), avec un nouvel établissement dédié pour le cas de démo (pas de réutilisation du "Centre de Formation Démo" existant, qui a son propre admin).

### Réalisé
- **`type_etablissement_secondaire` retiré** (colonne supprimée) : `Etablissement::typesEtablissement()`, les règles de validation (`Store`/`UpdateEtablissementRequest`) et l'exposition dans `EtablissementResource`/`UserResource` sont supprimés. Un établissement n'a plus qu'un seul type.
- **`CommandeController::valider`** : si un 2e type est choisi (champ `type_etablissement_secondaire` conservé côté requête pour ne pas casser le formulaire existant), un **2e établissement distinct** est créé (même durée d'abonnement) et l'admin (nouveau ou déjà existant) est rattaché aux deux via le pivot `etablissement_user` (même droits `acces.xxx` appliqués aux deux) — au lieu de cumuler les deux types sur un seul établissement.
- **`EtablissementFormModal`** : le sélecteur "2e type" est retiré (créer/modifier un établissement ne porte plus qu'un seul type) ; un texte explique désormais comment obtenir un 2e établissement pour le même admin (le créer, puis le rattacher via Comptes utilisateurs — flux déjà existant depuis le correctif multi-établissement du jour même).
- **Migration de bascule** (`split_type_etablissement_secondaire_into_separate_etablissements`) : pour chaque établissement ayant encore un type secondaire (données existantes), crée l'établissement distinct correspondant et y rattache chaque `admin_etablissement` du premier ; **comble aussi, pour tout utilisateur, la ligne de pivot manquante pour son établissement actif** (correctif général, au-delà du seul cas des 2 types — résout l'hypothèse documentée au précédent correctif sur les comptes de démo jamais rattachés). `ParametrageSeeder`/`UserSeeder` mis à jour pour créer ces lignes de pivot dès la création de chaque admin de démo (une nouvelle exécution de `migrate:fresh --seed` reproduit désormais un scénario multi-établissement fonctionnel pour `admin.universite@djaart.school`, sans dépendre d'un état de base déjà bricolé manuellement).
- Tests Feature : `CommandeEtMultiEtablissementTest` — le test de cumul de types est remplacé par un test vérifiant la création de 2 établissements distincts, le rattachement du même admin aux deux, et une bascule réussie de l'un à l'autre — 204/204 tests OK au total (suite complète, inchangée en nombre car un test a été remplacé, pas ajouté).
- Vérification : migration exécutée sur la base de développement réelle (pas seulement testée à vide) — `admin.universite@djaart.school` gère désormais concrètement 2 établissements distincts ("Université Démo DJAART" et "Université Démo DJAART — Centre de formation") avec un sélecteur de bascule fonctionnel ; `migrate:fresh --seed` re-vérifié propre après ce changement ; `npm run build`/`npm run lint` sans erreur.

### Hypothèses / écarts documentés
- **Le personnel non-admin (secrétaire, enseignant...) d'un établissement anciennement à 2 types cumulés perd l'accès aux écrans du 2e type** après la migration : seul l'admin est rattaché au nouvel établissement (demande explicite de l'utilisateur, "c'est toujours [l'admin] qui est admin"). Si un membre du personnel doit lui aussi intervenir dans les deux, il faut désormais le rattacher explicitement au 2e établissement (Comptes utilisateurs, avec le rôle de son choix pour cet établissement) — comportement cohérent avec le principe général du multi-établissement (rattachement délibéré, pas automatique).
- **Anomalie corrigée manuellement en cours de route** : la première exécution de la migration a échoué (erreur de syntaxe Eloquent, `wherePivot` non utilisable dans une sous-requête `whereHas`) après avoir déjà créé le nouvel établissement mais avant d'y rattacher l'admin ni de supprimer la colonne — l'établissement orphelin créé par cette tentative avortée a été supprimé manuellement (`tinker`) avant de corriger et relancer la migration avec succès ; aucune donnée utilisateur n'a été affectée (l'orphelin n'avait aucun utilisateur ni ligne de pivot rattachés).
- **Aucun outil de navigateur disponible dans cet environnement** (déjà constaté aux correctifs précédents) : vérification par tests automatisés + exécution réelle de la migration sur la base de développement + `migrate:fresh --seed` + `build`/`lint`, pas de parcours réel en navigateur pour ce tour.

## Correctif — Modélisation : `users.etablissement_id` n'est plus fiable pour l'appartenance à un établissement
- Statut : **terminé**

### Contexte
Retour utilisateur : `durandkamgang9@gmail.com`, ajouté avec des rôles différents dans plusieurs établissements, n'apparaissait dans la liste "Comptes utilisateurs" d'**aucun** des admins qui l'avaient ajouté (sauf un seul), et ne recevait aucun e-mail. L'utilisateur a explicitement demandé un audit de la modélisation des données (formes normales, ingénierie logicielle), pas seulement un correctif ponctuel.

**Diagnostic confirmé en base** : `durandkamgang9@gmail.com` a bien 3 lignes dans le pivot `etablissement_user` (comptable, comptable, enseignant dans 3 établissements distincts) — l'écriture du pivot fonctionne. Le bug est en lecture : `UserController::index()` et `UserPolicy` filtraient/comparaient encore sur `users.etablissement_id`, qui n'est que l'établissement **actif** du moment (celui où l'utilisateur a basculé en dernier, cf. correctif multi-établissement précédent) — pas la liste de ses appartenances. Un admin dont l'établissement actif ne correspond pas à celui de l'acteur au moment de la requête ne le voyait donc pas, même s'il l'a lui-même ajouté. Ce même défaut touchait aussi l'autorisation (`UserPolicy`) : un admin ne pouvait plus modifier le rôle/les droits d'un acteur qu'il gère légitimement dès que celui-ci avait basculé vers un autre établissement — et la suppression détruisait le compte entier, partagé avec d'autres admins.

**Absence d'e-mail** : aucune exception n'est journalisée (`storage/logs/laravel.log`) au moment de la création de ce compte ni de ses rattachements — l'envoi SMTP semble avoir été tenté sans erreur applicative. Cause probable : limite d'envoi/anti-spam côté Gmail après le volume de tests de ce projet, pas un bug de code (aucune preuve d'un défaut de la logique d'envoi, déjà couverte par les tests `Mail::fake()`). Non corrigé faute de pouvoir le diagnostiquer depuis cet environnement — à vérifier côté boîte Gmail expéditrice (dossier spam du destinataire, limite d'envoi de l'app password).

### Réalisé (modélisation)
- **`User::appartientA(int $etablissementId): bool`** : nouvelle méthode, source unique de vérité pour "cet acteur appartient-il à cet établissement" — utilise le pivot `etablissementsGeres` dès qu'il porte au moins une ligne (l'acteur a été ajouté via les points d'entrée applicatifs habituels) ; se replie sur `etablissement_id` **uniquement** si l'acteur n'a **aucune** ligne de pivot (compte créé hors de ces points d'entrée, ex. `tinker`/import direct) — évite qu'une donnée incomplète ne verrouille injustement un admin hors de son propre acteur, tout en donnant priorité au pivot dès qu'il existe.
- **`User::scopeAppartenantA()`/`scopeAvecRoleDans()`** : équivalents au niveau requête (pour les listes paginées), même logique pivot-avec-repli.
- **`UserPolicy::sameTenantOrSuperAdmin`** utilise désormais `appartientA()` au lieu de comparer `etablissement_id` — corrige `view`/`update`/`delete` pour un acteur partagé.
- **`UserController::index`** liste désormais les acteurs via `appartenantA()` (appartenance réelle), plus `where('etablissement_id', ...)` (pointeur actif) — un admin voit tous les acteurs qu'il gère, y compris ceux actuellement basculés ailleurs. Le filtre `?role=` utilise `avecRoleDans()` : il cherche le rôle **tenu dans mon établissement**, pas le rôle global "en direct" (qui peut être celui d'un tout autre établissement).
- **`UserController::update`/`updatePermissions`** : le rôle/les droits modifiés s'appliquent désormais à l'établissement **de l'acteur qui agit** (celui dans lequel il gère réellement la cible), pas à l'établissement actif de la cible qui peut être différent — le pivot de cet établissement est mis à jour dans tous les cas ; le rôle/droits "en direct" (Spatie) de la cible ne sont resynchronisés que si son établissement actif correspond bien à celui-ci (sinon elle les verra à sa prochaine bascule, comportement déjà existant).
- **`UserController::destroy`** : changement de sémantique important — un acteur partagé avec d'autres établissements n'est plus **supprimé entièrement** par un seul des admins qui le gèrent, seulement **détaché** de l'établissement de cet admin (son compte et ses autres appartenances restent intacts). Le compte n'est réellement supprimé que s'il ne reste plus rattaché à aucun établissement après ce détachement — comportement inchangé pour un acteur non partagé.
- Frontend : `UsersListPage.jsx` affiche désormais le rôle/établissement **tenus dans l'établissement de l'admin qui consulte** (`etablissements_geres[0]`, renvoyé scopé par le backend), pas le rôle/établissement global "actif" de la cible ; le bouton "Supprimer" est reformulé ("Retirer... de cet établissement ?") et affiche le message réellement renvoyé par l'API (suppression complète vs simple détachement).
- Tests Feature : 6 nouveaux tests dans `UserManagementTest` couvrant précisément le scénario signalé (acteur partagé, basculé vers un établissement tiers au moment de la requête) — liste, filtre par rôle, modification de rôle, modification des droits, suppression (détachement vs suppression complète) — 209/209 tests OK au total (suite complète).
- Vérification : `migrate:fresh --seed` propre (aucune migration de schéma dans ce correctif, uniquement des corrections de requêtes/autorisations) ; `npm run build`/`npm run lint` sans erreur.

### Hypothèses / écarts documentés
- **`users.etablissement_id` est conservé** (pas retiré du schéma) : il reste nécessaire comme pointeur "établissement actif" pour le tableau de bord/la session (cf. correctif multi-établissement précédent) — ce n'est pas en soi une erreur de modélisation de le garder, l'erreur était de l'utiliser aussi comme signal d'appartenance/autorisation ailleurs dans le code. Le corriger partout où c'était le cas (`UserPolicy`, `UserController`) résout le problème sans reprendre le schéma.
- **Repli sur `etablissement_id` uniquement si le pivot est totalement vide** (`appartientA()`) : décision délibérée pour la robustesse (comptes créés hors du flux applicatif normal) plutôt qu'un repli strict "pivot obligatoire" qui aurait été plus proche de la 3FN pure mais aurait risqué de verrouiller des admins hors de leurs propres acteurs en cas de donnée incomplète.
- **E-mail non reçu non résolu** (cf. ci-dessus) : aucune preuve d'un défaut de code trouvée (pas d'exception journalisée, mécanisme d'envoi identique et testé depuis plusieurs correctifs) — hypothèse la plus probable est une limite d'envoi ou un filtrage anti-spam côté Gmail, à vérifier manuellement (boîte d'envoi et boîte de réception concernées), hors de portée d'un correctif de code.
- **Base de développement réinitialisée par erreur pendant la vérification** : `migrate:fresh --seed` a été lancé par réflexe de vérification alors qu'aucune migration de schéma n'était en jeu dans ce correctif — cela a effacé les données de test réelles de l'environnement local (dont le compte `durandkamgang9@gmail.com` utilisé pour signaler ce bug). Sans conséquence sur le code ou une éventuelle base de production (jamais touchée par cet environnement), mais à recréer manuellement si ces données de test sont encore nécessaires.

## Correctif — Guide d'utilisation intégré, e-mail de production, préparation du déploiement LWS
- Statut : **terminé**

### Contexte
Trois demandes combinées : (1) une aide contextuelle dans l'application (fiches/info/popup) expliquant l'ordre des données à saisir et les prérequis des actions les plus complexes (« pour générer un bulletin, il faut d'abord quoi, cliquer où ») ; (2) basculer l'envoi des e-mails de la boîte Gmail utilisée en développement vers la boîte de production `contact@djaart.site` (SMTP `mail.djaart.site:465`) ; (3) un guide pour déployer sur l'hébergement LWS acheté (`api-school.djaart.site` → backend, `school.djaart.site` → frontend).

### Réalisé
- **Guide d'utilisation** (bouton « ? » dans la Topbar, toujours visible y compris sur mobile) : `GuideModal` présente, dans l'ordre technique réel de dépendance, les 18 étapes de paramétrage et d'usage courant (Établissement → Année académique → Départements/Filières → Niveaux → Classes → UE/Matières → Frais de scolarité → Inscriptions → Affectations → Séquences/Semestres → Saisie des notes → Bulletins → Relevés → Documents → Comptes utilisateurs), filtrées selon le rôle de l'acteur et le(s) type(s) d'établissement (même logique que `config/navigation.js`), avec un lien direct vers chaque écran (`src/config/guideSteps.js`).
- **Bandeaux d'aide contextuelle** (`HelpBanner`, repliable) ajoutés sur les 5 écrans aux prérequis les moins évidents : Affectations, Saisie des notes, Bulletins, Relevés, Frais de scolarité, Inscriptions — chacun explique précisément quoi faire avant quoi (ex. Bulletins : toutes les matières affectées doivent être saisies **et** verrouillées pour la séquence avant de pouvoir la clôturer, et la clôture échoue proprement en nommant la matière manquante).
- **E-mail de production** : `.env` (backend, jamais commité) basculé sur `mail.djaart.site:465` (SMTP implicite TLS, `MAIL_SCHEME=smtps`) avec `contact@djaart.site` — un envoi réel a été effectué depuis cet environnement pour confirmer que la configuration fonctionne (au-delà d'un simple `Mail::fake()`), comme pour la configuration Gmail initiale.
- **`DEPLOIEMENT.md`** (nouveau, racine du dépôt) : guide pas-à-pas pour LWS — création de la base MySQL, build et upload du frontend (avec la règle `.htaccess` de réécriture nécessaire pour une SPA React Router en hébergement Apache classique), upload du backend avec la contrainte de sécurité document-root ≠ racine du projet Laravel (deux méthodes selon que le panneau LWS autorise un document-root personnalisé ou non), variables d'environnement de production (notamment `SESSION_DOMAIN=.djaart.site` pour partager le cookie de session entre les deux sous-domaines distincts — différent du `SESSION_DOMAIN=null` utilisé en local/LAN), marche à suivre avec et sans accès SSH.
- Vérification : `npm run build`/`npm run lint` sans erreur ; suite backend complète 209/209 OK (aucune régression, changements limités au `.env` et au frontend) ; envoi SMTP réel confirmé.

### Hypothèses / écarts documentés
- **Guide statique (fiches/popup), pas une visite guidée interactive avec superposition sur l'interface** (type spotlight/tooltip attaché à chaque élément du DOM) : ce type de parcours pas-à-pas interactif nécessiterait une bibliothèque dédiée (ex. `react-joyride`/`shepherd.js`), une nouvelle dépendance non ajoutée ici faute de demande explicite en ce sens — le guide actuel couvre le contenu (quoi faire avant quoi, où cliquer) sans l'animation de superposition. À envisager comme évolution si souhaité.
- **Déploiement non exécuté par l'agent** : cet environnement n'a aucun accès (FTP/SSH/panneau) à l'hébergement LWS de l'utilisateur — `DEPLOIEMENT.md` est un guide à suivre manuellement, pas une action déjà effectuée. La configuration e-mail a en revanche été testée réellement, car elle ne dépend que du `.env` local.
- **Accès SSH LWS inconnu** : le guide couvre les deux cas (avec/sans SSH) faute de savoir quel plan LWS a été souscrit — à vérifier dans l'espace client LWS avant de suivre l'étape 6.

## Correctif — PWA installable, hors-ligne en lecture
- Statut : **terminé**

### Contexte
Demande : transformer le frontend en PWA installable (bouton flottant « Installer l'app ») fonctionnant « offline first ». Décision validée avec l'utilisateur (AskUserQuestion), vu la nature financière de l'app (paiements, inscriptions) : hors-ligne en **lecture seule** (coquille de l'app + dernières données consultées restent accessibles sans réseau) — les actions qui écrivent exigent une connexion et échouent proprement plutôt que d'être mises en file d'attente, pour éviter tout risque de double encaissement ou de conflit de données financières.

### Réalisé
- **`vite-plugin-pwa`** (stratégie `generateSW`, Workbox) : manifest (`name`, `theme_color`/`background_color` = bleu marine de la charte, `display: standalone`, `start_url: /dashboard`), icônes générées automatiquement depuis le logo existant via `@vite-pwa/assets-generator` (64/192/512, icône maskable, apple-touch-icon, favicon.ico — `pwa-assets.config.js`, régénérable via `npm run generate-pwa-assets`).
- **Hors-ligne en lecture** : le service worker précache la coquille de l'app (JS/CSS/HTML, `navigateFallback` pour que le routage côté client continue de fonctionner même sur un lien profond rechargé hors-ligne) et met en cache les réponses **GET** de l'API en stratégie `NetworkFirst` (donnée la plus fraîche dès que le réseau répond, repli sur la dernière version connue sinon) — un admin qui a déjà consulté une liste peut la revoir hors-ligne. Les requêtes **non-GET** (paiements, inscriptions, saisie de notes...) ne sont interceptées par aucune règle : elles échouent normalement hors-ligne (l'app affiche déjà ses messages d'erreur existants), au lieu d'être mises en file pour une synchronisation différée — décision délibérée, pas un oubli (cf. hypothèses ci-dessous).
- **Bouton flottant « Installer l'app »** (`InstallPwaButton.jsx`, monté globalement dans `App.jsx`) : intercepte `beforeinstallprompt` (Chrome/Edge/Android) pour déclencher l'installation directement au clic, se masque une fois l'app installée (`appinstalled` ou détection `display-mode: standalone` au chargement). iOS Safari n'expose aucune API d'installation programmatique : le bouton y ouvre à la place une astuce expliquant le chemin manuel (Partager → Sur l'écran d'accueil).
- Vérification : `npm run build` génère `sw.js`/`manifest.webmanifest` correctement (précaching de 11 entrées, règle `NetworkFirst` confirmée dans le service worker généré) ; `npm run preview` + requêtes réelles confirmant que le manifeste (`application/manifest+json`), le service worker et les icônes répondent tous en 200 depuis leurs URLs finales ; `npm run lint` sans erreur.

### Hypothèses / écarts documentés
- **Pas de synchronisation différée des écritures hors-ligne** (paiements/inscriptions/notes mis en file puis rejoués au retour du réseau) : chantier bien plus lourd et risqué pour des données financières (double encaissement si rejoué deux fois, conflit si la donnée a changé entre-temps côté serveur) — nécessiterait une conception dédiée (file d'attente, détection de conflits, écrans de résolution), refusé explicitement par l'utilisateur au profit d'un hors-ligne en lecture seule.
- **PWA active uniquement en production** (`npm run build` + service réel, pas `npm run dev`) — comportement par défaut de `vite-plugin-pwa` (`devOptions` non activé), pour éviter qu'un service worker actif ne complique le rechargement à chaud pendant le développement.
- **Icônes générées automatiquement à partir du logo existant** (empilement icône+texte+slogan, non carré) plutôt que d'un mark carré dédié — un fond bleu marine remplace le blanc par défaut pour éviter un carré disgracieux sur fond sombre, mais le rendu resterait plus net avec une icône carrée dédiée si l'utilisateur souhaite en fournir une plus tard.
- **Aucun outil de navigateur disponible dans cet environnement** (déjà constaté aux correctifs précédents) : vérification par `build`/`preview`/requêtes HTTP directes sur les artefacts générés, pas de test d'installation réelle depuis un navigateur (le comportement du bouton `beforeinstallprompt` et l'invite d'installation elle-même ne peuvent être confirmés que sur un appareil réel).

## Correctif — Coordonnées de contact réelles, archives de production prêtes à uploader
- Statut : **terminé**

### Contexte
Trois demandes combinées : (1) mettre les vraies coordonnées sur la page publique (`contact@djaart.site`, `+237 686865807`, jusque-là un e-mail provisoire `@djaart.school`) ; (2) préparer les identifiants de la base de données MySQL déjà créée et importée manuellement sur LWS (`c2816701c_djaart_school`) ; (3) livrer deux archives prêtes à dézipper directement sur le serveur, sans commande supplémentaire côté LWS — les migrations ne sont **pas** à rejouer (base déjà importée).

### Réalisé
- **Page publique** (`LandingPage.jsx`) : e-mail de contact remplacé par `contact@djaart.site` (section Contact + pied de page), ajout du numéro `+237 686 865 807` (lien `tel:`) dans la section Contact.
- **`.env` de production préparé** (jamais commité, vit uniquement dans l'archive livrée) : `APP_KEY` régénérée spécifiquement pour la production (jamais celle du poste de développement), `DB_DATABASE`/`DB_USERNAME`/`DB_PASSWORD` = identifiants LWS fournis, `MAIL_*` = `contact@djaart.site`/`mail.djaart.site`, `SESSION_DOMAIN=.djaart.site` (partage du cookie entre les deux sous-domaines), `APP_DEBUG=false`.
- **`api-school.zip`** et **`school.zip`** générés (`C:\Users\DIGITAL MARKET\Desktop\djaart-school-deploiement\`), prêts à être dézippés directement dans `/public_html/api-school` et `/public_html/school` :
  - Frontend : build de production (`VITE_API_URL=https://api-school.djaart.site`, nouveau `.env.production` committé — non sensible) + `.htaccess` de réécriture SPA.
  - Backend : extrait via `git archive` (garantit qu'aucun fichier de développement local — logs, cache, uploads de test — ne se retrouve dans l'archive, seuls les fichiers suivis par git plus `vendor/` généré à neuf comptent), `composer install --no-dev --optimize-autoloader`, restructuré avec `index.php` à la racine de l'archive (adapté pour pointer vers un sous-dossier `djaart-app/`) plutôt qu'à la racine du projet Laravel — nécessaire car aucune confirmation que le panneau LWS permette de choisir un document-root personnalisé ; un `.htaccess` dans `djaart-app/` bloque en plus tout accès web direct à ce dossier en défense en profondeur.
  - **Vérifiée par un vrai boot** : l'archive backend a été servie localement (`php -S`) et une requête a été envoyée jusqu'à l'échec attendu de connexion à la base (celle-ci n'étant naturellement joignable que depuis le serveur LWS, pas depuis cet environnement) — confirme que la restructuration `index.php`/chemins fonctionne correctement de bout en bout, pas seulement que les fichiers sont présents.
- **`DEPLOIEMENT.md`** réécrit pour refléter l'état réel : les deux archives sont prêtes, plus besoin de suivre les étapes de build manuellement — reste à vérifier le SSL sur les deux sous-domaines et, séparément, `php artisan storage:link` (nécessite un accès au serveur, non exécutable depuis cet environnement).
- Vérification : `npm run build`/`npm run lint` sans erreur ; URL API de production confirmée injectée dans le bundle JS ; contenu des deux archives inspecté (fichiers racine `.htaccess`/`index.php` présents, `.env` de production confirmé avec les bonnes valeurs sans être ré-affiché en clair).

### Hypothèses / écarts documentés
- **`DB_HOST=localhost`** dans le `.env` de production : hypothèse standard pour l'hébergement mutualisé LWS, **à vérifier** dans le panneau LWS (rubrique base de données) — certains plans utilisent un hôte MySQL différent. À corriger dans l'archive si nécessaire avant upload.
- **`php artisan storage:link` non exécuté** : nécessite un accès au serveur LWS (SSH ou création manuelle du lien via le gestionnaire de fichiers), impossible depuis cet environnement — tant qu'il n'existe pas, les logos/signatures/photos téléversés en production resteront inaccessibles publiquement (le reste de l'application fonctionne normalement).
- **Premier compte `super_admin` en production** : non créé par cet environnement (pas d'accès à la base LWS) — si la base importée manuellement n'en contient pas déjà un, il faudra le créer via `php artisan tinker` (accès SSH) ou directement via une requête SQL dans phpMyAdmin.
- **Aucun outil de navigateur ni accès réseau au serveur LWS depuis cet environnement** (déjà constaté aux correctifs précédents) : la vérification s'est limitée à un boot local de l'archive backend (confirmant la structure/le routage, pas la connexion réelle à la base distante) et à l'inspection du contenu des archives — aucun test end-to-end réel sur `school.djaart.site`/`api-school.djaart.site` n'a pu être effectué.

## Correctif — Déploiement LWS : archive illisible (permissions) + photos non affichées (storage:link)
- Statut : **terminé**

### Contexte
Après le premier déploiement réel sur LWS, deux problèmes distincts découverts en production (diagnostic mené conjointement avec l'utilisateur, qui n'avait accès qu'à son panneau LWS) : (1) `https://api-school.djaart.site/` renvoyait une erreur 500 brute, sans page d'erreur Laravel même avec `APP_DEBUG=true` ; (2) les photos de profil (et plus généralement tout fichier du disque "public" — logos, signatures) ne s'affichaient pas, l'utilisateur soupçonnant à raison le lien symbolique `storage:link` jamais exécuté (pas d'accès SSH confirmé).

### Diagnostic (1) — archive illisible
Démarche : test isolant (`phpinfo()` dans un fichier PHP nu à la racine d'`api-school`, fonctionnait) pour confirmer que PHP/Apache eux-mêmes marchaient, donc que le problème était dans le démarrage de Laravel — puis lecture du vrai journal d'erreurs PHP (distinct du journal d'accès Apache/Varnish, consulté en premier sans résultat exploitable), qui a révélé la cause exacte : `Failed opening required '.../vendor/symfony/deprecation-contracts/function.php': Permission denied`.

**Cause racine** : les archives de production avaient été générées avec `Compress-Archive` (PowerShell), qui n'écrit pas de bits de permission Unix valides dans les entrées du zip — un serveur Linux qui extrait une telle archive peut se retrouver avec des fichiers illisibles par le processus web. Un bug plus subtil est apparu en corrigeant ceci avec `ZipArchive` de PHP : `addEmptyDir()` stocke en interne le nom de l'entrée avec un slash final, et `setExternalAttributesName()` échoue silencieusement (retourne `false` sans erreur) si on ne lui passe pas ce même nom avec le slash — sans ce détail, les dossiers gardaient l'attribut par défaut de l'extension (`0777`) au lieu du `0755` voulu.

### Diagnostic (2) — storage:link
Plutôt que de simplement guider la création manuelle du lien symbolique (fragile en mutualisé, à refaire à chaque redéploiement, pas toujours possible sans SSH), recherche d'une solution ne dépendant plus d'aucune opération serveur : Laravel 12 embarque un mécanisme intégré méconnu (`FilesystemServiceProvider::serveFiles()`) qui enregistre automatiquement une route `GET /storage/{path}` pour tout disque local marqué `'serve' => true` — remplaçant complètement le besoin du lien symbolique. Le disque `local` du squelette Laravel 12 par défaut a cette option activée nativement, mais sert `storage/app/private` (reçus PDF, volontairement privés, déjà servis via une route authentifiée dédiée) ; les deux disques (`local` et `public`) auraient sinon revendiqué la même URI `/storage/{path}`, le premier enregistré (`local`) l'emportant toujours.

### Réalisé
- **`config/filesystems.php`** : `'serve' => true` déplacé du disque `local` (retiré) vers le disque `public` — les logos, signatures et photos de profil sont désormais servis nativement par Laravel à `/storage/{path}`, sans lien symbolique, que l'hébergement propose SSH ou non. Vérifié par un test réel (fichier déposé dans `storage/app/public/`, servi avec succès via une simulation fidèle du comportement Apache/`.htaccess`, sans aucun lien symbolique présent).
- **Génération des archives de production revue** : nouveau script (`build-zip-unix-perms.php`, hors dépôt git — outil de déploiement ponctuel) utilisant `ZipArchive` de PHP au lieu de `Compress-Archive`, avec permissions Unix explicites (fichiers `0644`, dossiers `0755`) correctement appliquées grâce au correctif du nom d'entrée avec slash final — vérifié entrée par entrée sur l'archive régénérée, y compris le fichier précédemment en échec.
- Les deux archives de production (`api-school.zip`, `school.zip`) régénérées avec ces deux correctifs et remises à l'utilisateur pour un nouveau déploiement.
- Vérification : suite de tests backend complète 209/209 OK (changement de configuration sans impact fonctionnel) ; boot réel de l'archive backend reconditionnée (comme au correctif précédent) ; route `/storage/{path}` testée et confirmée fonctionnelle localement sans lien symbolique.

### Hypothèses / écarts documentés
- **Fonctionnalité Laravel 12 non documentée dans les sources consultées jusqu'ici** (`FilesystemServiceProvider::serveFiles()`) — découverte en lisant directement le code du framework fourni (`vendor/laravel/framework`), pas dans la documentation officielle consultée en amont ; comportement confirmé par lecture du code source (`ServeFile`) et test réel, pas seulement supposé.
- **`build-zip-unix-perms.php` n'est pas commité au dépôt** (outil de déploiement ponctuel, dépendant de chemins locaux à l'environnement de préparation) — à formaliser en script réutilisable dans le dépôt si des déploiements réguliers sont prévus à l'avenir.
- **Aucun accès au serveur LWS depuis cet environnement** (déjà constaté aux correctifs précédents) : le diagnostic s'est fait entièrement via les journaux et tests que l'utilisateur a bien voulu copier depuis son panneau — reconfirmation du bon fonctionnement en production dépend de son prochain retour.

## Correctif — Connexion impossible sur mobile en production (CORS, variante www)
- Statut : **terminé**

### Contexte
Après déploiement, connexion impossible depuis un navigateur mobile (message "Impossible de joindre le serveur"), alors que le PC fonctionnait normalement. Diagnostic mené à distance (aucun accès à l'appareil de l'utilisateur) par élimination : certificat SSL valide, CORS/CSRF corrects testés depuis cet environnement, bundle JS déployé pointant vers la bonne URL d'API — écartant tour à tour l'hypothèse IPv6 (réseaux mobiles africains souvent IPv6 en priorité) et un résidu de cache/service worker (reproductible en navigation privée). Le test décisif : navigation directe vers `https://api-school.djaart.site/` depuis le téléphone fonctionnait, mais pas les appels JavaScript de l'app — isolant le problème aux requêtes cross-origin (CSRF + connexion), pas à la connectivité réseau elle-même.

**Cause trouvée** : `https://www.school.djaart.site` répond avec le même contenu que `https://school.djaart.site`, sans redirection entre les deux (configuration DNS/panneau LWS, pas un choix applicatif). Un visiteur arrivé sur la variante `www.` (favori, saisie manuelle, comportement d'un navigateur/opérateur) envoie un en-tête `Origin: https://www.school.djaart.site` à l'API, qui n'autorisait que `https://school.djaart.site` (sans www) en CORS — le navigateur bloque alors silencieusement la réponse, ce qu'`axios` remonte comme une erreur réseau générique plutôt qu'un blocage CORS explicite (d'où le message "Impossible de joindre le serveur", techniquement correct du point de vue du code mais trompeur sur la vraie cause).

### Réalisé
- **`.env` de production** (`CORS_ALLOWED_ORIGINS`, `SANCTUM_STATEFUL_DOMAINS`) élargi pour accepter les deux variantes (`school.djaart.site` et `www.school.djaart.site`) — corrige la connexion quelle que soit celle utilisée par le visiteur. `SESSION_DOMAIN=.djaart.site` (déjà en place) couvrait déjà les deux, seul CORS/Sanctum bloquait.
- Archive de production (`api-school.zip`) régénérée avec ce correctif, remise à l'utilisateur.

### Hypothèses / écarts documentés
- **`www.school.djaart.site` et `school.djaart.site` restent deux origines distinctes sans redirection canonique** — corrige le blocage immédiat mais laisse un doublon (impact SEO potentiel, cohérence des liens partagés) ; non traité ici faute de portée explicitement demandée. Ajouter une redirection 301 permanente vers une seule forme canonique (au niveau du `.htaccess` du frontend ou du panneau LWS) réglerait cela proprement si souhaité dans un prochain tour.
- **HTTP (non chiffré) répond aussi sans redirection forcée vers HTTPS** — remarqué en creusant ce problème, non corrigé (hors périmètre de la demande initiale) ; à traiter de la même manière qu'une éventuelle redirection www.
- **Diagnostic mené entièrement par élimination à distance** (déjà constaté aux correctifs de déploiement précédents) : chaque hypothèse a été vérifiée soit depuis cet environnement (certificat, CORS, CSRF, contenu déployé), soit via des tests précis demandés à l'utilisateur sur son propre appareil — la confirmation définitive que la connexion fonctionne désormais sur mobile dépend de son prochain retour.

## Correctif — Un échec d'envoi d'e-mail ne doit plus jamais faire échouer l'action métier
- Statut : **terminé**

### Contexte
Rapporté en production : soumettre une demande de démo ou une commande depuis la page publique renvoyait une erreur serveur générique, avec en cause exacte (retrouvée dans les journaux) `Expected response code "250/251/252" but got code "550", with message "550 L-RF1 This recipient has been reported recently as unexistant"` — le serveur SMTP rejetait un des destinataires de la notification interne.

### Diagnostic
Tous les envois d'e-mail de l'application sont synchrones (`Mail::send()`, décision actée dès la Phase 5 pour éviter qu'un message reste bloqué faute de worker de file d'attente actif) : une exception de livraison non rattrapée remonte donc jusqu'au contrôleur et casse toute la requête HTTP (500), **alors que l'enregistrement métier (demande, commande...) est déjà en base à ce moment-là** — l'utilisateur voit une erreur pour une action pourtant réussie. Pire : plusieurs de ces envois se trouvent à l'intérieur d'une transaction DB (`DB::transaction`) couvrant toute une opération — un encaissement, la clôture d'une séquence pour une classe entière, la validation d'une commande — une exception de mail y provoque un "rollback" complet de tout le travail déjà fait, pas seulement une réponse HTTP en erreur.

### Réalisé
- **`App\Support\Mailer::envoyer(callable $envoi): bool`** : nouveau point de passage unique pour tout envoi d'e-mail — capture et journalise (`Log::error`) tout incident de livraison sans jamais le relancer ; renvoie un booléen pour que l'appelant puisse adapter sa réponse si l'e-mail portait une information qui n'existe nulle part ailleurs (mot de passe généré).
- Appliqué aux **13 points d'envoi** de l'application : `DemandeDemoController`/`CommandeController` (bug rapporté), `UserController` (création de compte, réinitialisation de mot de passe, droits modifiés, rattachement à un nouvel établissement), et les notifications aux apprenants dans `RecuService`/`PaiementService`/`BulletinService`/`ReleveService` (reçu de paiement, validation d'inscription, bulletin prêt, relevé disponible) — ces quatre derniers étaient les plus critiques : un e-mail d'apprenant invalide aurait pu faire échouer (et annuler) un encaissement ou la clôture de toute une classe.
- **Messages de réponse adaptés** pour les flux qui livrent un mot de passe généré (`CompteCreeMail`, `MotDePasseReinitialiseMail`, `CommandeValideeMail` — seul canal existant, jamais affiché à l'admin) : l'action reste un succès (le compte est créé/mis à jour), mais le message précise explicitement que l'e-mail n'a pas pu être envoyé, pour que l'admin sache qu'il doit relancer une réinitialisation une fois le problème résolu, plutôt que de laisser un compte silencieusement inutilisable.
- Tests : nouveau `Tests\Unit\MailerTest` (le helper capture l'exception et ne la relance jamais), nouveau test dans `DemandeDemoTest` reproduisant précisément le bug rapporté (mail simulé en échec via un mock du destinataire renvoyant l'erreur SMTP 550 réelle — la soumission reste un succès 201, la demande est bien enregistrée) — 212/212 tests OK au total (suite complète).
- Vérification : suite de tests complète + boot réel de l'archive de production reconditionnée (nouvelle classe `App\Support\Mailer` confirmée autoloadable dans le `vendor/` optimisé régénéré, échec attendu uniquement sur la connexion à la base distante inaccessible depuis cet environnement — comme aux vérifications de déploiement précédentes) ; archive de production régénérée et remise à l'utilisateur.

### Hypothèses / écarts documentés
- **La cause du rejet SMTP lui-même (quel destinataire précisément, pourquoi "reported as unexistant") n'a pas pu être identifiée depuis cet environnement** (aucun accès à la base de production) — l'hypothèse la plus probable est qu'un compte `super_admin` de la base importée manuellement conserve encore une adresse de démonstration (`@djaart.school`, utilisée en local, jamais une vraie boîte) comme destinataire des notifications de demande de démo/commande. Le correctif présent empêche que cela casse l'application, mais ne garantit pas que la notification soit réellement livrée tant que l'adresse en base n'est pas corrigée — recommandé de vérifier/mettre à jour l'e-mail du ou des comptes `super_admin` en production.
- **Aucun accès à la base de production ni à l'appareil de l'utilisateur depuis cet environnement** (déjà constaté aux correctifs de déploiement précédents) : la confirmation définitive que la soumission d'une demande de démo/commande fonctionne désormais en production dépend du prochain retour de l'utilisateur.

## Phases suivantes
Voir `DJAART_SCHOOL_CLAUDE_CODE_BUILD_PLAN.md` section 6 pour le détail des phases 10 à 11. Prochaine étape : **Phase 10**.
