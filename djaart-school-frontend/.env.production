# Utilisé automatiquement par `vite build` (mode production) — frontend et
# backend vivent sur deux sous-domaines distincts en production, contrairement
# au dev local où l'URL du backend est déduite de l'hôte de la page.
VITE_API_URL=https://api-school.djaart.site
